# counter-increment` é o atributo que define o valor que ira adicionar ou subtrair do contador existente.



























---

## ➕ O que é `counter-increment`?

Pense no `counter-increment` como um **botão de somar** ➕

Toda vez que um elemento aparece na página, ele diz:

> “Some +1 no contador”

---

## 🧠 O que ele faz exatamente?

Quando o CSS vê:

```css
li {
  counter-increment: item;
}
```

Ele entende:

> “Sempre que um `<li>` existir, aumente o contador `item`.”

📈 O número sobe automaticamente.

---

## ⚠️ Importante

👉 **Ele não cria o contador**  
👉 **Ele não mostra o número**

Se não existir um `counter-reset` antes, **nada funciona** ❌

---

## 🔢 Somar outros valores

Você não é obrigado a somar +1.

```css
counter-increment: item 2;
```

📊 Agora ele soma **+2** a cada vez:

```
2, 4, 6, 8...
```

---

## 🔻 Diminuir valores (sim, pode!)

```css
counter-increment: item -1;
```

📉 O contador vai **diminuindo**:

```
5, 4, 3, 2...
```

---

## 📦 Incrementar vários contadores

```css
counter-increment: capitulo 1 subcapitulo 1;
```

📚 Dois contadores sendo alterados ao mesmo tempo.

---

## 🧩 Onde normalmente usamos?

Normalmente você usa em:

- `li`
    
- `p`
    
- `h2`, `h3`
    
- qualquer elemento que “repete”
    

```css
li {
  counter-increment: passo;
}
```

Cada `<li>` vira um novo número 🧮

---

## ❌ O que `counter-increment` NÃO faz

❌ Não cria contador  
❌ Não exibe números  
❌ Não define início

Ele **só soma ou subtrai** ➕➖

---

## 🧠 Resumo mental rápido

> **counter-increment** = aumenta ou diminui o valor do contador ➕🔢

---

## 📘 Definição técnica (simples)

> **`counter-increment`** é uma propriedade CSS usada para **incrementar ou decrementar o valor de um contador existente** sempre que o elemento é renderizado. ➕➖🎨

Se quiser, o próximo passo natural é:  
👉 `counter()` explicado separado  
👉 `counters()` (aninhado)  
👉 exemplo real tipo capítulos e subcapítulos 📖

É só mandar 😄🚀