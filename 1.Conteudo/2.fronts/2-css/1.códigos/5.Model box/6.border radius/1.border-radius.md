# **`border-radius`** é o **atributo** que define **o arredondamento dos cantos da borda de um elemento**, deixando-os curvos ou circulares.
























> `border-radius` é uma **propriedade do CSS** que controla os arredondamentos **das pontas** de uma tag.
---

### 📦 Imagine uma caixa comum com cantos retos

Por padrão, todos os elementos HTML têm **cantos quadrados**:

```html
<div style="width: 200px; height: 100px; background-color: lightblue;"></div>
```

Essa caixa terá cantos retos (90 graus).  
Mas... e se quisermos deixar os cantos **arredondados**?  
É aí que entra o `border-radius`!

---

## ✅ Exemplo prático básico

```html
<!DOCTYPE html>
<html>
<head>
  <style>
    .caixa-arredondada {
      width: 200px;
      height: 100px;
      background-color: lightblue;
      border-radius: 20px; /* Arredonda todos os 4 cantos */
    }
  </style>
</head>
<body>

  <h2>Caixa com cantos arredondados</h2>
  <div class="caixa-arredondada"></div>

</body>
</html>
```

---

## 🔍 Explicando

```css
border-radius: 20px;
```

- Arredonda todos os **4 cantos** da caixa em 20 pixels.
    
- Quanto maior o valor, mais arredondado fica.
    
- Se o valor for grande o suficiente, a caixa vira um **círculo** (se for quadrada).
    

---

## 🎯 Exemplos de uso real

### 🔹 Botões com aparência suave

```html
<button style="padding: 10px 20px; border-radius: 8px;">Clique aqui</button>
```

Esse botão fica com **cantos suaves**, muito usado em sites modernos.

---

### 🔹 Deixar uma imagem redonda (como foto de perfil)

```html
<img src="https://via.placeholder.com/100" style="border-radius: 50%;">
```

- Usando `border-radius: 50%` em uma imagem quadrada, ela vira um **círculo perfeito**.
    
- Ótimo para **fotos de perfil**!
    

---

## 🧱 Variações e formas de usar

Você pode controlar **cada canto separadamente**:

```css
border-radius: 10px 20px 30px 40px;
```

Ordem dos cantos:

```
top-left   top-right
bottom-right  bottom-left
```

Exemplo:

```css
border-radius: 10px 20px 30px 40px;
/* significa:
   canto superior esquerdo: 10px
   canto superior direito: 20px
   canto inferior direito: 30px
   canto inferior esquerdo: 40px
*/
```

---

## 🧪 Brinque com os valores

Você pode experimentar com esses estilos para ver os efeitos visuais:

```css
border-radius: 50px;
border-radius: 0 50px 0 50px;
border-radius: 30% 70% 70% 30%;
```

---

## 📝 Resumo

|Propriedade|O que faz|
|---|---|
|`border-radius`|Arredonda os cantos de um elemento|
|`border-radius: 20px`|Arredonda todos os 4 cantos em 20px|
|`border-radius: 50%`|Transforma elementos quadrados em círculos|
|`border-radius: TL TR BR BL`|Controla cada canto individualmente|

---

Se quiser, posso te dar um **desafio criativo**: criar uma "foto de perfil com borda arredondada e sombra", que parece coisa de site profissional. Quer tentar?