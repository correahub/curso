# **`vw`** é a **unidade de medida** relativa ao tamanho da largura disponível da tela.




































### 🧠 O que é `vw`?

`vw` significa **"viewport width"**  
👉 Em português: **largura da janela visível do navegador**.

---

### 📏 Como funciona?

- **1vw = 1% da largura da viewport (tela visível)**.
    
- Ou seja:
    
    - `100vw` = largura total da tela
        
    - `50vw` = metade da largura
        
    - `10vw` = 10% da largura
        

---

### 🔹 Exemplo prático:

```css
div {
  width: 100vw;
  background-color: lightgreen;
}
```

Essa `<div>` vai ocupar **100% da largura da tela** de lado a lado, mesmo se o elemento pai for menor.

---

### ✅ Quando usar `vw`?

- Para criar **layouts responsivos** que se ajustam ao tamanho da tela.
    
- Para elementos que precisam **ocupar a largura total**, como banners, slides, ou seções.
    
- Para definir tamanhos de fonte fluidos:
    

```css
h1 {
  font-size: 5vw; /* o texto cresce ou diminui com a largura da tela */
}
```

---

### ❗ Cuidados:

- Se você usar `100vw` e ainda houver **scroll lateral**, pode ser que o navegador mostre uma barra de rolagem indesejada.
    
- Isso acontece porque **100vw ignora a barra de rolagem vertical**. Ela também ocupa espaço!
    

👉 Solução: às vezes usar `width: 100%` é melhor dentro de containers.

---

### 🆚 Comparação com outras unidades

|Unidade|Base|Exemplo|
|---|---|---|
|`px`|Valor fixo|Sempre igual|
|`%`|Relativo ao **elemento pai**|Pode variar|
|`vw`|Relativo à **largura da tela**|50vw = metade da tela visível|

---

### 📐 Parentes do `vw`

|Unidade|Significado|
|---|---|
|`vh`|Altura da viewport|
|`vmin`|Menor valor entre `vh` e `vw`|
|`vmax`|Maior valor entre `vh` e `vw`|

---

### 💡 Dica final:

Unidades como `vw` e `vh` são muito poderosas quando combinadas com `flexbox`, `grid` e media queries — permitindo **sites realmente responsivos**, sem precisar ficar calculando em pixels.