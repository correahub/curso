# **`rem`** é a **unidade de medida relativa** ao tamanho do fonte-size do elemento (`html`)**, permanecendo consistente em toda a página.





























### 🧠 O que é `rem`?

A unidade `rem` significa:

> **r**oot **em**

Ou seja, ela é **relativa ao tamanho da fonte do elemento raiz**, que é o elemento `<html>`.

---

### 📏 Como funciona?

Por padrão, os navegadores definem:

```css
html {
  font-size: 16px;
}
```

Então:

```css
1rem = 16px
2rem = 32px
0.5rem = 8px
```

Simples, direto e **não depende dos elementos pais**.

---

### 🔹 Exemplo prático:

```css
html {
  font-size: 16px;
}

h1 {
  font-size: 3rem; /* 3 × 16px = 48px */
}

p {
  margin-top: 1.5rem; /* 1.5 × 16px = 24px */
}
```

---

### ✅ Vantagens do `rem`

- 🔁 **Consistente:** Sempre baseado no `<html>`, nunca muda dependendo do pai.
    
- 📱 **Responsivo:** Se você alterar o `font-size` do `<html>`, todos os elementos que usam `rem` se adaptam automaticamente.
    
- ♿ **Acessibilidade:** Ótimo para permitir que o usuário aumente ou diminua o zoom da página com clareza.
    

---

### ❗ Diferença entre `rem` e `em`

| Unidade | Base                     | Cresce com o pai atual? | Exemplo                                   |
| ------- | ------------------------ | ----------------------- | ----------------------------------------- |
| `em`    | Elemento pai atual       | ✅ Sim                   | Pode "multiplicar" se tiver muitos níveis |
| `rem`   | Elemento raiz (`<html>`) | ❌ Não                   | Sempre constante, baseado no root         |

---

### 🧪 Exemplo com ambos:

```css
html {
  font-size: 10px;
}

.container {
  font-size: 2em; /* 2 × 10px = 20px */
}

.container p {
  font-size: 1.5em; /* 1.5 × 20px = 30px */
}

.container .title {
  font-size: 2rem; /* 2 × 10px = 20px */
}
```

- `.title` usa `rem`, então sempre será 20px (mesmo dentro de `.container`)
    
- Já o `p` cresceu acumulando os `em`
    

---

Se quiser, posso gerar um exemplo visual interativo comparando `em`, `rem` e `px`. Deseja isso?