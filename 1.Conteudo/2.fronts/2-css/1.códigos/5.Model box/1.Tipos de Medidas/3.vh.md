# **`vh`** é a **unidade de medida** relativa ao tamanho da altura disponível da tela.

























### 🧠 O que é `vh`?

`vh` significa **"viewport height"**  
👉 Em português: **altura da janela visível do navegador**.

---

### 📏 Como funciona?

- **1vh = 1% da altura total da tela visível**
    
- Então:
    
    - `100vh` = 100% da altura da **viewport** (tela visível do navegador)
        
    - `50vh` = metade da altura da viewport
        
    - `10vh` = 10% da altura da viewport
        

---

### 🔹 Exemplo prático:

```css
div {
  height: 100vh;
  background-color: lightblue;
}
```

Esse `<div>` vai ocupar **toda a altura da tela**, de cima a baixo.

---

### 📱 O que é "viewport"?

É a **área visível da página no seu navegador**, sem rolagem.  
Quando você redimensiona o navegador, a viewport muda de tamanho.

> 💡 Em dispositivos móveis, a altura da `viewport` pode mudar quando a barra de endereço aparece ou desaparece.

---

### ✅ Quando usar `vh`?

- Para **criar seções de tela cheia** (como um banner que ocupa a tela inteira).
    
- Para ajustar **elementos dinamicamente**, sem depender de pixels fixos.
    
- Para construir **layouts responsivos**.
    

---

### ❗ Cuidados:

- Em **dispositivos móveis**, a altura da `viewport` pode variar com a rolagem.
    
    - Por exemplo: a barra de endereço do navegador aparece e desaparece.
        
    - Isso pode fazer o `100vh` parecer menor do que o esperado.
        
    - Solução: usar JavaScript para medir a altura real, se necessário (posso te mostrar como, se quiser).
        

---

### 🆚 Comparação com outras unidades

|Unidade|Base de cálculo|Exemplo|
|---|---|---|
|`px`|Tamanho fixo|Sempre igual|
|`%`|Relativo ao **elemento pai**|Pode variar|
|`vh`|Relativo à **altura da viewport**|100vh = altura total da tela|

---

### 📐 Outros parentes do `vh`

|Unidade|Significado|
|---|---|
|`vw`|Viewport **width** (largura)|
|`vmin`|Menor valor entre `vh` e `vw`|
|`vmax`|Maior valor entre `vh` e `vw`|
