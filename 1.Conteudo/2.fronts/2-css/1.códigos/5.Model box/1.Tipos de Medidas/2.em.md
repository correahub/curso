# **`em`** é a **unidade de medida** **relativo ao tamanho do fonte size do elemento pai**, crescendo ou diminuindo conforme esse valor.


























### 🧠 O que é `em`?

A unidade `em` é **relativa ao tamanho da fonte do elemento pai**.  
Ou seja, 1em = **100% do tamanho da fonte herdada**.

---

### 📏 Como funciona na prática?

#### 🔹 Exemplo 1: Tamanho de fonte

```css
body {
  font-size: 16px;
}

p {
  font-size: 2em;
}
```

Nesse caso:

- O `body` tem `16px` de fonte.
    
- O `p` usa `2em`, ou seja:
    
    - 2 × 16px = **32px**
        

---

### 🔹 Exemplo 2: Margem com `em`

```css
body {
  font-size: 20px;
}

div {
  margin: 2em;
}
```

- A `margin` do `div` será `2 × 20px = 40px`.
    

Mesmo que a margem não seja uma fonte, ela **usa o tamanho da fonte como base de cálculo**.

---

### ❗ E se o elemento filho usar `em` também?

Aí o valor é **cumulativo**!

```css
body {
  font-size: 16px;
}

div {
  font-size: 1.5em; /* 1.5 × 16px = 24px */
}

p {
  font-size: 2em; /* 2 × 24px = 48px */
}
```

O `<p>` neste caso terá **48px de tamanho de fonte**, porque está usando `2em` baseado no `div`, que já aumentou a base para 24px.

---

### 💡 Dica: `em` é útil quando você quer que **tudo cresça proporcionalmente**, mantendo as proporções mesmo em acessibilidade, zoom, ou responsividade.

---

### 🆚 Comparação com outras unidades:

|Unidade|Base de cálculo|Exemplo|
|---|---|---|
|`px`|Fixo (pixels)|`16px` é sempre 16px|
|`em`|Fonte do elemento pai|`2em` pode mudar dependendo de onde está|
|`rem`|Fonte da raiz (`html`)|`2rem` sempre = 2 × `font-size` do `html` (ex: 2 × 16px = 32px)|

---

Se quiser, posso te mostrar um exemplo visual em HTML comparando `em`, `rem` e `px`. Quer ver?