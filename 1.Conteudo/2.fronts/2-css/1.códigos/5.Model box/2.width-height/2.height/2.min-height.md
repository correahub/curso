# **`min-height`** é o **atributo** que define **a altura mínima de um elemento**, impedindo que ele fique menor que esse valor.
























🧱 O que são _min-height_ e _max-height_?

Lembra das **caixinhas** mágicas do HTML?  
Agora vamos controlar a **altura** delas!

Essas duas propriedades funcionam assim:

- **min-height** → altura **mínima** da caixinha
    
- **max-height** → altura **máxima** da caixinha
    

É como dizer:

👉 “Caixinha, você NÃO pode ficar mais baixinha do que isso!”  
👉 “E também NÃO pode ficar mais alta do que isso!”

---

# 🟩 min-height (altura mínima)

É como combinar:

"Ei caixinha... mesmo se quiser encolher, você **não pode ficar menor** que este tamanho!"

```css
div {
  min-height: 200px; /* Nunca fica mais baixa que 200px */
}
```

📌 Mesmo se o conteúdo for pequeno ou a tela for pequena, ela **nunca** fica menor que 200px.

---

# 🟥 max-height (altura máxima)

Agora você diz:

"Caixinha, você pode crescer... mas **não pode passar** desse tamanho!"

```css
div {
  max-height: 400px; /* Nunca fica mais alta que 400px */
}
```

📌 Mesmo se o conteúdo for enorme, ela **não cresce além** de 400px.  
Se o conteúdo for grande demais, ela cria **scroll** (rolagem).

---

# 🎒 Comparação simples (como um pote de massinha)

Imagine um pote:

- **min-height** → o pote nunca amassa tanto a ponto de ficar muito baixinho
    
- **max-height** → o pote nunca fica tão alto que não cabe no armário
    

Ele tem um **tamanho mínimo** e um **tamanho máximo** permitido.

---

# 🎨 Exemplo prático

```html
<div style="
  background: lightblue;
  min-height: 150px;
  max-height: 300px;
  overflow: auto;
">
  Eu nunca fico menor que 150px e nem maior que 300px!
  Se tiver conteúdo demais, eu criarei uma barrinha de rolagem!
</div>
```

---

# 🧸 Atalhos pra lembrar

|Propriedade|O que ela controla|
|---|---|
|**min-height**|A menor altura permitida|
|**max-height**|A maior altura permitida|

📏 min → mínimo  
📐 max → máximo