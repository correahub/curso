# **`min-width`** é o **atributo** que define **a largura mínima de um elemento**, impedindo que ele fique menor que esse valor.





















 🧱 O que são _min-width_ e _max-width_?

Imagine novamente que um elemento é uma **caixinha**.

Agora vamos usar dois superpoderes:

- **min-width** → diz o **mínimo** que a caixinha pode ficar de largura
    
- **max-width** → diz o **máximo** que a caixinha pode crescer de largura
    

É como dizer para a caixinha:

👉 **"Você não pode ficar menor do que isso!"**  
👉 **"Você também não pode ficar maior do que isso!"**

---

# 🟦 min-width (largura mínima)

É como dizer:  
"Caixinha, NÃO fique mais apertadinha do que esse tamanho."

```css
div {
  min-width: 200px; /* Nunca fica menor que 200px */
}
```

📌 Mesmo que a tela fique bem pequena, a caixinha **nunca vai encolher** abaixo desses 200px.

---

# 🟥 max-width (largura máxima)

Agora você fala:  
"Caixinha, NÃO cresça mais do que isso."

```css
div {
  max-width: 500px; /* Nunca fica maior que 500px */
}
```

📌 Mesmo que a tela seja enorme, a caixinha **não vai crescer além** de 500px.

---

# 🎒 Exemplo fácil (como se fosse uma mochila)

Imagine uma mochila:

- **min-width** = o tamanho mínimo dela para caber um livro
    
- **max-width** = o tamanho máximo para não ficar gigante e pesada
    

Assim ela nunca fica pequena demais nem grande demais.

---

# 🎨 Exemplo que você vê na prática

```html
<div style="
  background: lightcoral;
  min-width: 200px;
  max-width: 400px;
">
  Eu nunca fico menor que 200px e nem maior que 400px!
</div>
```

---

# 🧸 Dica simples pra nunca esquecer:

|Propriedade|Significado|
|---|---|
|**min-width**|Largura **mínima** da caixinha|
|**max-width**|Largura **máxima** da caixinha|

🎯 min = mínimo  
🎯 max = máximo