# **`absolute`** é um **valor** do atributo `position` que define o **posicionamento de um elemento, removendo do fluxo normal da página fazendo ele ficar posicionando dentro do seu ancestral posicionado mais próximo que não seja `static`**, permitindo movê-lo usando `top`, `right`, `bottom` e `left`.























---

# 🎯 O que é **position: absolute**?

Imagine um bloquinho (elemento HTML).  
Com `position: absolute`, ele **solta a mão dos outros bloquinhos** e fala:

> “Agora eu fico onde você mandar, sem seguir o lugar normal da página!” 😎📦✨

Ele sai completamente do fluxo normal da página.

---

# 🧱 Como ele se comporta?

Com `position: absolute`:

### ✅ 1. Ele **não ocupa espaço** no layout

Os outros elementos fingem que ele nem existe.  
É como se ele tivesse desaparecido da fila. 👻

---

### ✅ 2. Ele se posiciona a partir do **parente posicionado**

Ele procura o primeiro _pai_ que tenha `position: relative`, `absolute`, `fixed` ou `sticky`.

- Se encontrar → ele se posiciona baseado naquele pai.
    
- Se **não** encontrar → ele usa **a tela inteira (body)** como referência.
    

É tipo uma criança perguntando:  
“Quem é meu responsável? Ah, é você! Vou ficar do seu lado!” 😄

---

### ✅ 3. Ele pode usar `top`, `left`, `right`, `bottom`

Essas propriedades CONTROLAM totalmente onde ele vai aparecer.

---

### 💡 Exemplo

```html
<style>
  .pai {
    position: relative;
    width: 200px;
    height: 200px;
    background: lightblue;
  }

  .filho {
    position: absolute;
    top: 20px;
    left: 10px;
    background: yellow;
  }
</style>

<div class="pai">
  <div class="filho">Eu fico preso ao pai! 😄</div>
</div>
```

O quadradinho amarelo (`absolute`) vai se posicionar dentro do quadrado azul (`relative`).

---

# 🧠 Como imaginar isso facilmente?

Pense assim:

- **static** → “Fico no meu lugar.”
    
- **relative** → “Fico no lugar, mas posso me mover um pouco.”
    
- **absolute** → “Não sigo mais o fluxo, vou pra onde você mandar!”
    

---

# 📘 Definição técnica simples (como você pediu)

**position: absolute**  
👉 “Remove o elemento do fluxo normal e o posiciona em relação ao ancestral posicionado mais próximo usando top, right, bottom e left.” ⚙️📌😄

---
