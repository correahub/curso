
# **`static`** é o **valor** do atributo position que define o **posicionamento padrão de um elemento**, onde ele segue o fluxo normal da página e ignora `top`, `right`, `bottom` e `left`.


























---

# 🧱 O que é **position: static**?

Imagine que você tem vários bloquinhos (elementos HTML) empilhados na tela. Cada bloquinho tem seu lugar natural, seguindo a ordem que você colocou no código, um embaixo do outro, bem comportadinhos.

👉 **position: static** significa:  
**“Fique no seu lugar normal, sem querer ser especial.”** 😄

É o **valor padrão** da propriedade `position`.  
Ou seja, mesmo que você não escreva `position: static`, o navegador já está usando esse valor por padrão.

---

# 🎯 Como ele se comporta?

Com `position: static`:

- O elemento **não pode ser movido** usando `top`, `left`, `right`, `bottom`.  
    (Se você tentar, eles simplesmente não funcionam.)
    
- Ele segue o **fluxo normal da página**.
    
- Ele não cria um novo contexto de empilhamento (z-index).
    
- Ele fica exatamente onde o navegador colocou, como um bom soldadinho 📦➡️📦➡️📦.
    

---

# 📦 Explicação técnica simples (como você pediu)

**position: static**  
👉 “Valor padrão. O elemento segue o fluxo normal do layout e ignora as propriedades top, bottom, left e right.” ⚙️📘

---

# 🌟 Exemplo bem simples

```html
<style>
  div {
    position: static;
    top: 50px; /* NÃO funciona */
  }
</style>

<div>Eu fico aqui mesmo 😅</div>
```

Mesmo você pedindo para ele descer 50px, ele não desce — porque `static` não aceita deslocamentos.

---
