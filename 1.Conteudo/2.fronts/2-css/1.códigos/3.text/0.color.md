
# **`color`** é o **atributo** que define **a cor do texto** de um elemento.



























# 🎨 O que é a propriedade **color**?

A propriedade **color** no CSS é usada para mudar a **cor do texto**.  
Só isso! 😄💬

É como se você pegasse um **lápis de cor** 🖍️ e dissesse:

👉 “Quero que essas palavras fiquem vermelhas!” ❤️  
👉 “Quero que esse título fique azul!” 💙  
👉 “Quero que esse aviso fique verde!” 💚

E o CSS pinta o texto pra você! ✨

---

# ✍️ Como se escreve?

Você escreve assim:

```css
elemento {
  color: nome-ou-código-da-cor;
}
```

---

# 🎨 Exemplos coloridos!

### 🔴 1. Usando nomes de cores:

```css
p {
  color: red;
}
```

➡️ Todo o texto do parágrafo vai ficar **vermelhinho** 😄

---

### 🔵 2. Usando códigos hexadecimais:

```css
h1 {
  color: #0000ff;
}
```

➡️ O texto fica **azul**  
(Esse código é como um número secreto que representa uma cor!)

---

### 🟢 3. Usando RGB:

```css
span {
  color: rgb(0, 255, 0);
}
```

➡️ O texto fica **verde**  
(RGB é como misturar tinta vermelha, verde e azul pra criar cores!)

---

### 🟣 4. Usando RGBA:

```css
div {
  color: rgba(128, 0, 128, 0.5);
}
```

➡️ Um roxinho **meio transparente**, como se fosse tinta com água 💧

---

# 🧒 Pensando como criança:

É como se seu site fosse um **livro de pintar** 📚🎨  
E o **color** fosse o lápis que você usa para pintar as palavras.

Se você diz:

👉 **color: pink;**  
O texto fica **rosa**, como um algodão doce! 🍭💗

---

# 🎯 Importante!

- **color NÃO muda o fundo**, só o **texto**.  
    Para o fundo, usamos **background-color** 🎨
    
- A cor do texto **passa para os filhos**  
    (Se você colocar color no body, todo o texto dentro dele herda essa cor.)
    

---

# 🧪 Exemplo fácil e completo:

HTML:

```html
<p>Eu sou um texto colorido! ✨</p>
```

CSS:

```css
p {
  color: purple;
}
```

Resultado:

💜 O texto fica **roxo**!  
Como se você tivesse passado um lápis de cor violeta nele 😄

---
