# **`text-shadow`** é o **atributo** que define **uma sombra aplicada ao texto**, controlando posição, desfoque e cor da sombra.




















## 🧠 O que é `text-shadow`?

> `text-shadow` é uma **propriedade CSS** que adiciona uma **sombra ao texto**.

É como colocar uma **luz invisível** que faz o texto projetar uma sombra atrás dele, o que pode deixá-lo mais bonito, estilizado ou mais visível.

---

### 🎯 Para que serve?

- Melhorar a leitura de textos claros sobre fundos escuros (ou vice-versa).
    
- Criar efeitos visuais como "brilho", "neon", "profundidade" etc.
    
- Dar um toque moderno e estiloso a títulos, botões ou mensagens importantes.
    

---

## ✅ Exemplo prático simples

```html
<!DOCTYPE html>
<html>
<head>
  <style>
    .texto-com-sombra {
      font-size: 40px;
      color: navy;
      text-shadow: 2px 2px 4px gray;
    }
  </style>
</head>
<body>

  <h2 class="texto-com-sombra">Texto com Sombra</h2>

</body>
</html>
```

---

## 🔍 Explicando o `text-shadow`

```css
text-shadow: 2px 2px 4px gray;
```

Cada valor tem um papel:

```
text-shadow: deslocamento-horizontal deslocamento-vertical desfoque cor;
```

|Parte|O que faz|
|---|---|
|`2px`|Move a sombra 2 pixels para a direita|
|`2px`|Move a sombra 2 pixels para baixo|
|`4px`|Borrão: quanto maior, mais suave e espalhada a sombra|
|`gray`|Cor da sombra|

---

## 🌟 Exemplos práticos para brincar

### 🔹 Sombra escura para texto claro

```css
text-shadow: 1px 1px 2px black;
```

Ideal para texto branco em fundo claro:

```html
<p style="color: white; background: #ccc; text-shadow: 1px 1px 2px black;">
  Texto com sombra escura
</p>
```

---

### 🔹 Efeito Neon (brilho colorido)

```css
text-shadow: 0 0 5px #0ff, 0 0 10px #0ff, 0 0 20px #0ff;
```

Cria um efeito de **neon azul**:

```html
<h1 style="color: #0ff; background: black; text-shadow: 0 0 5px #0ff, 0 0 10px #0ff, 0 0 20px #0ff;">
  Neon!
</h1>
```

---

### 🔹 Texto em relevo ou 3D leve

```css
text-shadow: 1px 1px 0 #ccc;
```

Dá uma sensação de relevo ou profundidade no texto.

---

## ✍️ Variações e múltiplas sombras

Você pode usar **mais de uma sombra** separando com vírgulas:

```css
text-shadow: 1px 1px 2px black, -1px -1px 2px gray;
```

Cada sombra será aplicada sobre a anterior. Isso permite efeitos avançados como brilho, contorno ou destaque.

---

## ❗ Observações

- `text-shadow` **só funciona em textos**. Para caixas, usamos `box-shadow`.
    
- Funciona em **qualquer tag com texto**: `<h1>`, `<p>`, `<button>`, `<a>`, etc.
    
- Você pode usar **cores com transparência** (ex: `rgba(0,0,0,0.5)`).
    

---

## 📝 Resumo

|Propriedade|O que faz|
|---|---|
|`text-shadow`|Adiciona sombra ao texto|
|`2px 2px 4px gray`|Direita, baixo, borrado, cor|
|Várias sombras|Separadas por vírgulas para efeitos especiais|
|Efeitos comuns|Neon, relevo, brilho, destaque|
