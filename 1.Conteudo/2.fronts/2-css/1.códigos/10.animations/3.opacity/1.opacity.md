# opacity é um atributo que define o quanto um elemento é transparente ou visível.

----
# 👻 O que é `opacity`?

Pense no `opacity` como um **controle de transparência**, igual quando você pega uma imagem e deixa ela “sumindo” aos poucos.

É como se o elemento estivesse ficando **invisível devagarinho**.  
🖼️➡️👻

---

# 🌈 Definição simples

**`opacity` controla o quanto um elemento é transparente ou visível.**

---

# 🔢 Valores do opacity

Ele vai de **0 até 1**:

- **1** → totalmente visível (nada transparente) 🔆
    
- **0.5** → meio transparente, 50% 👓
    
- **0** → totalmente invisível 😶‍🌫️
    

---

# 🧪 Exemplos

### ✨ Tornar um elemento metade transparente

```css
div {
  opacity: 0.5;
}
```

Ele ainda existe, mas você vê através dele.

---

### 👻 Tornar o elemento invisível

```css
div {
  opacity: 0;
}
```

Ele continua ocupando espaço na página, mas some visualmente.

---

# 🎯 Detalhes importantes

- `opacity` afeta **todo o elemento** e tudo dentro dele (texto, imagens, filhos).  
    → ou seja, **é global**.
    
- Mesmo com `opacity: 0;`, o elemento **continua clicável**, a menos que você use algo como `pointer-events: none`.
    
- Ele não remove o elemento do layout:  
    ⚠️ _ele fica invisível, mas continua lá!_
    

---

# 📘 Definição técnica simples (como você pediu)

**Opacity define o nível de transparência de um elemento, variando de totalmente visível (1) a completamente invisível (0), sem alterar seu espaço no layout.** ⚙️👁️✨

---

Se quiser, posso explicar a diferença entre **opacity**, **visibility: hidden** e **display: none** 😄✨