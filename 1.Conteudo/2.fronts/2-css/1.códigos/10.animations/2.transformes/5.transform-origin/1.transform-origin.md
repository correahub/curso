
# Transform-origin é o atributo que define onde as transformações do elemento começam.






















---

# 🎯 O que é `transform-origin`?

Pense que todo elemento tem um **ponto fixo** no seu corpo, como um **pivô**.  
Quando você usa `rotate`, `scale` ou `skew`, o elemento gira ou cresce **em torno desse ponto**.

O `transform-origin` serve para **mudar esse ponto de onde o elemento é transformado**.

É como escolher **onde está o “prego” no qual o elemento está pendurado** 🪝✨

---

# 🌈 Definição simples

**`transform-origin` define o ponto exato de onde as transformações (como rotate, scale, skew) começam, controlando o centro da transformação.** 

---

# 🌀 Por padrão

O valor padrão é:

```css
transform-origin: center center;
```

Ou seja:  
➡️ **centro horizontal**  
⬆️ **centro vertical**

Esse é o “pivô” padrão.

---

# 📍 Exemplos para entender

### 🔄 1. Girar a partir do canto superior esquerdo

```css
transform-origin: top left;
transform: rotate(45deg);
```

Agora o elemento **gira preso no canto** igual uma porta girando na dobradiça. 🚪✨

---

### ❤️ 2. Girar a partir do centro (padrão)

```css
transform-origin: center;
```

O giro é bem equilibrado, como uma moeda girando no centro 🪙🔄

---

### ⚓ 3. Usar porcentagens

```css
transform-origin: 0% 100%;
```

Isso significa:

- **0% → à esquerda**
    
- **100% → embaixo**
    

É o canto inferior esquerdo.

---

### 📐 4. Usar pixels

```css
transform-origin: 50px 20px;
```

Aqui você escolhe **um ponto exato dentro do elemento** como pivô.

---

# 🎯 Por que isso é importante?

Porque sem o transform-origin:

- animações podem parecer estranhas,
    
- giros sempre acontecem no centro,
    
- escalas crescem para todos os lados igualmente.
    

Com `transform-origin` você controla exatamente **onde o efeito começa**.

---

# 📘 Definição técnica simples (como você gosta)

**Transform-origin especifica o ponto de referência interno do elemento a partir do qual as transformações são aplicadas.** ⚙️🎯✨