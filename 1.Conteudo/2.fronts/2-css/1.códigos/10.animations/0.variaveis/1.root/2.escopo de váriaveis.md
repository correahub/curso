
# Variável só é valida do lugar onde foi criada para baixo, por isso variáveis locais só vale para ela e para os filhos, enquanto root vale para a página inteira.





















---

## 📦 O que é **escopo de variáveis**?

**Escopo** é o **lugar onde a variável funciona** 🗺️

👉 Em outras palavras:

> _Quem pode usar essa variável?_ 🤔

---

## 🏠 Comparação fácil (bem didática)

Imagine que sua casa é o site 🏡  
E cada cômodo é um pedaço do CSS 🚪

- Variável criada na **sala** → todo mundo da casa pode usar
    
- Variável criada no **quarto** → só quem está no quarto pode usar
    

Isso é **escopo** 😄

---

## 🌍 Escopo GLOBAL (variável para o site inteiro)

Quando você cria a variável no `:root`, ela vira **global** 🌎✨

### Exemplo:

```css
:root {
  --cor-principal: blue;
}
```

➡️ Essa variável pode ser usada em **qualquer lugar** do CSS:

```css
h1 {
  color: var(--cor-principal);
}

button {
  background: var(--cor-principal);
}
```

✔ Todo mundo pode usar  
✔ É o escopo **mais amplo**

---

## 🚪 Escopo LOCAL (variável limitada)

Se você cria a variável **dentro de um seletor**, ela só funciona **ali dentro** 🔒

### Exemplo:

```css
.card {
  --cor-card: green;
}
```

Essa variável **só existe dentro da `.card`** 👇

```css
.card p {
  color: var(--cor-card);
}
```

❌ Fora da `.card`, ela **não funciona**

```css
h1 {
  color: var(--cor-card); /* NÃO funciona */
}
```

---

## 🔁 Escopo funciona em CASCATA

CSS é tipo herança de família 👨‍👩‍👧‍👦✨

- Elementos **filhos** herdam variáveis do **pai**
    
- Mas o pai **não herda do filho**
    

### Exemplo:

```css
.section {
  --cor-texto: red;
}

.section p {
  color: var(--cor-texto);
}
```

✔ O `<p>` consegue usar  
❌ A `.section` não pode usar variável criada no `<p>`

---

## ⚠️ Se duas variáveis têm o mesmo nome?

Vale a **mais próxima** 📍👀

### Exemplo:

```css
:root {
  --cor: blue;
}

.card {
  --cor: green;
}

.card p {
  color: var(--cor);
}
```

🎯 Resultado: **verde**, porque a `.card` está mais perto do `<p>`

---

## 🧠 Regra de ouro (muito importante!)

> **A variável vale do lugar onde foi criada para baixo, nunca para cima** ⬇️🚫⬆️

---

## 🧩 Resumo rápido

|Onde a variável foi criada|Onde funciona|
|---|---|
|`:root`|Em todo o site 🌍|
|Em uma classe|Só nessa classe e filhos|
|Em um elemento|Só nele e filhos|

---

## 📌 Definição técnica (simples, como você pediu)

**Escopo de variáveis é a área do CSS onde uma variável pode ser usada, indo do seletor onde foi declarada para seus elementos filhos.** 📦⬇️🧠

---

Se quiser, posso te explicar:

- escopo no **JavaScript** (comparação) ⚙️
    
- erro comum com variáveis CSS ❌
    
- quando usar variável global vs local 🎯
    

Só falar 😄