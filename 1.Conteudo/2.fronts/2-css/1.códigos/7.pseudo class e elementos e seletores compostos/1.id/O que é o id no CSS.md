# id é o **atributo** que define **um identificador único para um elemento**, usado para selecioná-lo no CSS, JavaScript ou como âncora na página.






















---

# 🌟 O que é o **id** no CSS?

**O `id` é como um nome único que você dá para um elemento no HTML.**  
Pensa assim: se você tivesse vários irmãos e sua mãe chamasse “Ei, vocês aí!”, todos responderiam. Mas se ela chamasse pelo **seu nome**, só **você** responderia. 🎯👦

O `id` faz isso: ele permite que você fale **com um único elemento específico** na página.

---

# 🎨 Como funciona o `id` no HTML?

Você coloca o `id` dentro da tag:

```html
<div id="caixa"></div>
```

Aqui o nome desse elemento é **caixa**, e só ele tem esse nome.

---

# 🎨 Como acessar o `id` no CSS?

Você usa o símbolo **#**:

```css
#caixa {
  width: 200px;
  height: 200px;
  background-color: blue;
}
```

O `#caixa` significa:  
👉 “Pegue aquele elemento que tem **id="caixa"** e aplique essas regras.”

---

# ⚠️ Regrinha super importante!

💥 **Um id deve ser único na página.**  
Isso significa **não repetir o mesmo id em dois elementos diferentes**.

Se repetir, o navegador fica confuso, como quando duas crianças têm exatamente o mesmo nome na mesma sala 😅

---

