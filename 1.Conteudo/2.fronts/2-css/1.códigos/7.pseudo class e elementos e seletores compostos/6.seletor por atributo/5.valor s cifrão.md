# **`[atributo$="valor"]`** é um **seletor por atributo** que define **elementos cujo valor do atributo termina com a string especificada**.





















```css
[atributo$="valor"]
```

Como sempre, explicação simples e divertida, tipo pra criança entender 🧒✨

---

## 🧠 O que significa `[atributo$="valor"]`?

Esse seletor **escolhe elementos cujo atributo TERMINA com um certo valor**.

📢 O cifrão `$` quer dizer:

> "**Termina com** esse valor"

---

## 🧸 Imaginando com etiquetas:

Cada brinquedo tem uma etiqueta com uma palavra.  
Você diz:

> “Pinte só os brinquedos cuja etiqueta **termina com** `.jpg`.”

---

## 🎯 Exemplo real:

### CSS:

```css
[src$=".jpg"] {
  border: 3px solid purple;
}
```

### HTML:

```html
<img src="foto1.jpg" alt="gato" />
<img src="imagem.png" alt="cachorro" />
<img src="carro.jpg" alt="carro" />
```

---

### ✅ O que acontece?

- O primeiro e o terceiro `<img>` têm `src` que **termina com `.jpg`** → ganham borda roxa ✅
    
- O segundo `<img>` termina com `.png` → não muda ❌
    

---

## 🧠 Resumindo:

> `[atributo$="valor"]`  
> Seleciona todos os elementos que têm o valor do atributo **terminando com** aquilo que você quer.

---

## 🧪 Outro exemplo prático:

### CSS:

```css
[href$=".pdf"] {
  color: red;
  font-weight: bold;
}
```

### HTML:

```html
<a href="documento.pdf">Download PDF</a>
<a href="pagina.html">Página normal</a>
<a href="arquivo.pdf">Outro PDF</a>
```

➡️ Os links que terminam com `.pdf` vão ficar vermelhos e em negrito ✅

---

## ⚠️ Dica importante:

- Esse seletor é útil para identificar tipos de arquivos, extensões, ou finais de URLs.
    

---

## 📌 Tabela rápida de comparação:

|Seletor|O que faz|
|---|---|
|`[atributo="valor"]`|Valor **exatamente igual**|
|`[atributo^="valor"]`|Valor que **começa com** esse texto|
|`[atributo$="valor"]`|Valor que **termina com** esse texto ✅|
|`[atributo*="valor"]`|Valor que **contém** esse texto em qualquer lugar|

---

Quer que eu continue com o seletor `[atributo*="valor"]` que pega quando o valor **contém** algo?