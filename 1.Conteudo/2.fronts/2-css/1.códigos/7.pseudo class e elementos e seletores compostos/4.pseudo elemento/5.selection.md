# **`::selection`** é um **pseudo-elemento** que define **a aparência do texto quando ele é selecionado pelo usuário**, como cor do fundo e da letra.

















---

### 🧒 Imagina isso:

Você está lendo um texto na tela e resolve **clicar e arrastar o mouse para selecionar uma parte**, tipo quando você vai copiar algo.

Normalmente, quando você faz isso, **o texto selecionado fica azul**, né?

> Com o `::selection`, você pode **mudar essa cor azul para qualquer outra cor que quiser!** 🎨

---

### 💡 O que é `::selection`?

`::selection` é um **pseudo-elemento CSS** que **permite mudar o estilo do texto quando ele é selecionado pelo usuário**, como quando você segura o botão do mouse e passa por cima do texto.

---

### 📦 Exemplo super simples:

HTML:

```html
<p class="texto">
  Esse texto vai ficar colorido quando você selecionar com o mouse!
</p>
```

CSS:

```css
.texto::selection {
  background-color: yellow;
  color: black;
}
```

### 🖱️ O que vai acontecer?

Quando você **clicar e arrastar com o mouse sobre esse texto**, a parte selecionada vai ficar com:

- **Fundo amarelo**
    
- **Texto preto**
    

Igual um marca-texto! 🟨🖍️

---

### ✅ O que dá pra mudar com `::selection`?

Você **pode mudar**:

- `background-color` (cor de fundo)
    
- `color` (cor do texto)
    
- `text-shadow` (sombra no texto, se quiser enfeitar)
    

---

### ❌ O que **não funciona** no `::selection`?

Você **não pode usar**:

- `font-size`
    
- `display`
    
- `padding`
    
- `margin`
    
- `border`
    
- `transform`
    
- E outras propriedades "complexas"
    

Só coisas **simples e rápidas de aplicar**!

---

### 🎨 Exemplo divertido:

```css
p::selection {
  background-color: hotpink;
  color: white;
}
```

Quando alguém selecionar o texto, ele vai ficar **rosa choque com letra branca**. Super estiloso! 💖

---

### 📚 Exemplo completo:

```html
<!DOCTYPE html>
<html lang="pt-br">
<head>
  <style>
    p::selection {
      background-color: lightgreen;
      color: darkgreen;
    }
  </style>
</head>
<body>
  <p>Selecione esse texto com o mouse e veja a mágica acontecer!</p>
</body>
</html>
```

---

### 🧠 Dica final:

O `::selection` é como **um pincel mágico** que você usa para mudar **a cor do texto quando alguém toca nele com o mouse**. É uma forma legal de deixar seu site mais bonito e único! 😍

---

Se quiser, posso mostrar como aplicar `::selection` para **todo o site** de uma vez só! Quer ver?