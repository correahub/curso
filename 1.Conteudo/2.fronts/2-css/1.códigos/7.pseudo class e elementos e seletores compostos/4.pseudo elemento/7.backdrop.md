# **`::backdrop`** é um **pseudo-elemento** que define **o fundo externo exibido quando um elemento como um popover, dialog ou modal está aberto**, permitindo aplicar estilos de destaque ou escurecimento.



















---

### 👶 Explicando como se fosse pra uma criança:

Imagina que você está no teatro 🎭. Quando vai começar a peça, as luzes se apagam e uma **cortina escura aparece atrás** dos personagens para chamar atenção só para eles.

No HTML, essa cortina é o `::backdrop`.

> Ela **escurece o fundo** e faz todo mundo prestar atenção no popover (ou modal).

---

### 🧪 Onde o `::backdrop` funciona?

Ele funciona em elementos que **podem cobrir a tela**, como:

- `<dialog>`
    
- Elementos com `popover` (quando usado com `popover="auto"` ou `manual`, com `showPopover()`)
    

---

### 🧪 Exemplo prático com popover:

```html
<style>
  [popover] {
    padding: 20px;
    border-radius: 10px;
    background: white;
    border: 2px solid black;
  }

  /* Estilo da "cortina" de fundo */
  [popover]::backdrop {
    background-color: rgba(0, 0, 0, 0.6); /* fundo escuro e transparente */
  }
</style>

<!-- Botão para abrir o popover -->
<button popovertarget="alerta">Mostrar Alerta</button>

<!-- Popover com backdrop -->
<div popover id="alerta">
  ⚠️ Atenção! Isso é importante.
</div>
```

---

### 💡 O que faz o `::backdrop`?

- Cria **um fundo escurecido** atrás do popover (ou `<dialog>`).
    
- Deixa o popover em destaque, tipo uma janelinha flutuando.
    
- É **estilizável no CSS**: você pode mudar cor, opacidade, usar imagens, etc.
    

---

### ⚠️ Importante:

- O `::backdrop` **só aparece** se o popover ou dialog estiver sendo exibido como **modal**.
    
- O popover precisa ser chamado de forma que **cubra a tela**, senão o `::backdrop` nem aparece.
    
- Com popovers simples acionados por `popovertarget`, **não aparece por padrão** — apenas com certos estilos (ex: `position: fixed` + largura/tamanho de tela).
    
