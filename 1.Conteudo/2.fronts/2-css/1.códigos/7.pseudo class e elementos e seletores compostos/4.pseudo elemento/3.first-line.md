# `::first-line` é um **pseudo-elemento** que define **a primeira linha de texto de um elemento, permitindo aplicar estilos apenas a essa linha.





















---

### 🧒 Imagina isso:

Você escreveu um **texto grandão** num caderno. Agora, você quer deixar **a primeira linha desse texto mais bonita**, tipo com uma cor diferente ou uma letra maior.

> É isso que o `::first-line` faz: **ele muda o estilo só da primeira linha do parágrafo**!

---

### 💡 O que é `::first-line`?

`::first-line` é um **pseudo-elemento CSS** que **permite mudar o estilo da primeira linha visível de um texto**.

Não é a primeira frase ou a primeira palavra… é **a linha que aparece primeiro na tela**, como se fosse a primeira "linha escrita no papel".

---

### 📦 Exemplo simples:

HTML:

```html
<p class="texto">
  Era uma vez um dragão muito grande que vivia numa caverna escura no topo de uma montanha distante...
</p>
```

CSS:

```css
.texto::first-line {
  color: blue;
  font-weight: bold;
  font-size: 20px;
}
```

### 🖥️ O que aparece na tela?

A **primeira linha do parágrafo vai estar azul, em negrito e maior**! O resto do texto continua normal.

---

### ✅ O que dá pra mudar com `::first-line`?

Você pode usar **estilos que afetam o texto**, como:

- `color`
    
- `font-size`
    
- `font-weight`
    
- `font-style`
    
- `text-transform`
    
- `letter-spacing`
    
- `word-spacing`
    

---

### ❌ O que **não funciona** com `::first-line`?

Você **não pode** usar coisas que mexem no "bloco" do texto, como:

- `margin`
    
- `padding`
    
- `display`
    
- `background-color` (às vezes não funciona em todos os navegadores)
    

---

### 🧠 Dica final:

O `::first-line` é perfeito para dar **destaque à primeira linha** de um texto, como em **contos, cartas, notícias** ou qualquer coisa onde você queira deixar a **entrada mais bonita e chamativa**.

---

### 🎨 Exemplo criativo:

```css
p::first-line {
  color: red;
  text-transform: uppercase;
}
```

Isso vai deixar a primeira linha **toda em letras maiúsculas e vermelha**. Tipo um grito:  
**ERA UMA VEZ UM LUGAR LONGE, LONGE...**

---

Quer um exemplo com várias linhas ou combinando com outras pseudo-classes? Posso te mostrar! 😄