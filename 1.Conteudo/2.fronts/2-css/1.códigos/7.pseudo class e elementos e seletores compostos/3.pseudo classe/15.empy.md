# **`:empty`** é uma **pseudo-classe** que define **elementos que não possuem nenhum conteúdo**, nem texto nem elementos filhos.























---


Ele pega apenas elementos **totalmente vazios**.

---

## 📘 Explicação simples (no estilo das suas definições):

**Pseudo-class `:empty` seleciona elementos que não possuem conteúdo algum.** 🕳️✨

---

# 🧒 Explicando como para uma criança:

Imagine uma caixinha.  
Se dentro dessa caixinha não tem:

- nenhum brinquedo (nenhum elemento),
    
- nenhum papel (nenhum texto),
    
- nem um grãozinho de poeira (nenhum espaço),
    

então o CSS diz:

👀 “Essa caixinha está completamente vazia, vou aplicar o estilo de `:empty`!”

Mas se tiver **qualquer coisa**, até um espaço invisível, já não funciona mais.

---

# 🧪 Exemplo simples

### 🔗 HTML:

```html
<div class="a"></div>
<div class="b">Olá</div>
<div class="c"> </div> <!-- Contém espaço, então NÃO é empty -->
```

### 🎨 CSS:

```css
div:empty {
  border: 2px dashed red;
  background: #ffecec;
}
```

👉 Somente o primeiro `<div class="a"></div>` será estilizado.  
O `<div class="c"> </div>` não é empty porque contém um espaço!

---

# ⭐ Coisa importante sobre `:empty`

**Se houver espaços, quebras de linha ou qualquer texto invisível, o elemento deixa de ser empty.**

Exemplo que NÃO funciona:

```html
<div>
</div>
```

Mesmo parecendo vazio, a quebra de linha conta como conteúdo!

---

# 🎯 Definição técnica simples:

**`:empty` é uma pseudo-class que seleciona elementos que não têm filhos nem conteúdo de texto.** 🕳️✨

---

Se quiser, posso te explicar **`:not()`**, **`:nth-child()`**, **`:first-child`**, **`:is()`** ou qualquer outra pseudo-class! 😄🔥