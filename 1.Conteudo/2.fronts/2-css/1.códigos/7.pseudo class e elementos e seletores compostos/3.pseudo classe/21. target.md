# **`:target`** é uma **pseudo-classe** que define **o elemento cujo `id` corresponde ao fragmento da URL**, sendo ativada quando um link âncora aponta para ele.


















---

## 📌 O que é `:target` no CSS?

### 👉 Definição simples:

**`:target` é um seletor CSS que seleciona o elemento que foi “apontado” pelo link com `#` na URL.** 🔗🎯

No final:  
👉 Ele funciona quando **algo vira o alvo da página**.

---

## 🧠 Como isso funciona na prática?

### 1️⃣ Você tem um elemento com **id**

```html
<section id="sobre">
  Conteúdo sobre nós
</section>
```

### 2️⃣ Você tem um link apontando para ele

```html
<a href="#sobre">Ir para Sobre</a>
```

Quando você clica no link, a URL vira:

```
site.com/#sobre
```

Agora o elemento `#sobre` vira o **alvo (target)** 🎯

---

## 🎨 Usando `:target` no CSS

```css
:target {
  background-color: yellow;
}
```

📌 Quando o usuário clicar no link:

- O navegador vai até o elemento
    
- **E o CSS `:target` será aplicado só nele**
    

---

## 🧪 Exemplo completo

### HTML

```html
<a href="#caixa">Mostrar caixa</a>

<div id="caixa">
  Eu sou a caixa 🎁
</div>
```

### CSS

```css
#caixa {
  display: none;
}

#caixa:target {
  display: block;
  background-color: lightblue;
}
```

### 🔥 O que acontece?

- A caixa começa escondida ❌
    
- Ao clicar no link → ela aparece ✅
    
- Tudo isso **sem JavaScript** 😮✨
    

---

## 🧩 Onde o `:target` é MUITO usado?

✅ Abrir e fechar conteúdo  
✅ Simular modais simples  
✅ FAQs (perguntas e respostas)  
✅ Navegação interna da página  
✅ Animações com CSS puro

---

## ⚠️ Regras importantes

📌 Só funciona com:

- `id`
    
- links com `#id`
    

📌 Apenas **um elemento** pode ser `:target` por vez

---

## 🧠 Resumo infantil 🧒

🟡 O link diz: “Ei, quero ir ali!”  
🎯 O elemento vira o alvo  
🎨 O CSS `:target` pinta só esse alvo

---

## 📘 Definição técnica (como você pediu 👇)

> **`:target` é um pseudo-seletor CSS que seleciona o elemento cujo `id` corresponde ao fragmento (`#`) atual da URL.** 🎯🔗✨

Se quiser, posso te mostrar:

- um **modal com :target**
    
- um **menu sanfona**
    
- ou comparar `:target` vs JavaScript
    

É só dizer 😄