# **`:placeholder-shown`** é uma **pseudo-classe** que define **o estado de um campo de formulário quando o texto de placeholder está visível**, ou seja, quando o usuário ainda não digitou nada.
















---

 ✏️ O que é **`:placeholder-shown`** no CSS?

**`:placeholder-shown` é uma pseudo-class que seleciona um campo de formulário (`input` ou `textarea`) quando o placeholder está visível.**

Isso só acontece quando:

- o campo **está vazio**, e
    
- o texto do **placeholder** está aparecendo.
    

---

## 📘 Explicação simples (no estilo das suas definições):

**Pseudo-class `:placeholder-shown` pega elementos cujo placeholder está sendo exibido.** ✏️✨

---

# 🧒 Explicando como para uma criança:

Imagine que você tem uma caixinha para escrever seu nome.  
Quando ela está vazia, aparece uma frase fraquinha lá dentro, tipo:

📝 “Digite seu nome…”

Essa frase é o **placeholder**.

O CSS olha para essa caixinha e pensa:

👀 “Opa, o texto cinzinha ainda está aparecendo? Então vou aplicar um estilo especial!”

Mas quando você começa a digitar qualquer coisa, o CSS diz:

🚫 “Agora o placeholder sumiu, então não uso mais o estilo.”

---

# 🧪 Exemplo simples

### 🔗 HTML:

```html
<input type="text" placeholder="Digite algo...">
```

### 🎨 CSS:

```css
input:placeholder-shown {
  border: 2px dashed blue;
  background: #eef6ff;
}

input:not(:placeholder-shown) {
  border: 2px solid green;
  background: #eaffea;
}
```

👉 Enquanto o placeholder está aparecendo → borda azul  
👉 Quando você digita algo → borda verde

---

# ⭐ Exemplo super comum (labels flutuantes)

```css
input:placeholder-shown + label {
  opacity: 0.5;
  transform: translateY(10px);
}

input:not(:placeholder-shown) + label {
  opacity: 1;
  transform: translateY(-10px);
}
```
