# **`:invalid`** é uma **pseudo-classe** que define **o estado de um elemento de formulário quando seu valor não é válido**, de acordo com as regras definidas (como `required`, `type` ou `pattern`)
























## ❌ `:invalid` — “Algo errado, tente de novo!”

Agora, se você digitar **algo errado**, como:

```
banana
```

Isso **não é um e-mail válido**, então ela fica **inválida**, e o CSS pode mostrar um aviso:

```css
input:invalid {
  border: 2px solid red;
}
```

📌 **O que acontece?**  
Se estiver errado, a borda vai ficar **vermelha**, avisando que você precisa corrigir.