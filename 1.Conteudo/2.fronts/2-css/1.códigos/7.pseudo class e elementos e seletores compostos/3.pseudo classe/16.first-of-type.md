#  **`:first-of-type`** é uma **pseudo-classe** que define **o primeiro elemento de um determinado tipo dentro de um elemento pai**, ignorando outros tipos de elementos.




























---

## O que é `:first-of-type`? 🧩

👉 **`:first-of-type` é um seletor do CSS** que escolhe **o PRIMEIRO elemento de um TIPO específico** dentro de um elemento pai.

📌 **Palavra-chave importante:**

- Ele **não olha se é o primeiro filho**
    
- Ele olha se é o **primeiro daquele TIPO** (tag)
    

---

## Diferença importante ⚠️

### `:first-child` ❌ vs `:first-of-type` ✅

Vamos ver um exemplo:

```html
<div>
  <h1>Título</h1>
  <p>Parágrafo 1</p>
  <p>Parágrafo 2</p>
</div>
```

### ❌ `p:first-child`

```css
p:first-child {
  color: red;
}
```

👉 **Não funciona**, porque o primeiro filho da `div` é um `<h1>`, não um `<p>`.

---

### ✅ `p:first-of-type`

```css
p:first-of-type {
  color: blue;
}
```

👉 **Funciona!**  
Porque ele pega **o primeiro `<p>`**, ignorando o `<h1>`.

---

## Como pensar nisso (modo criança 🧠✨)

Imagine uma caixa 🎁 (`div`) com brinquedos diferentes:

- 🧸 Urso (`h1`)
    
- 🚗 Carrinho (`p`)
    
- 🚗 Carrinho (`p`)
    

🔹 `:first-child` → pega **o primeiro brinquedo da caixa**  
🔹 `:first-of-type` → pega **o primeiro brinquedo daquele tipo**

---

## Outro exemplo prático 👇

```html
<section>
  <span>Texto</span>
  <span>Texto</span>
  <p>Parágrafo</p>
  <p>Parágrafo</p>
</section>
```

```css
span:first-of-type {
  color: red;
}

p:first-of-type {
  color: green;
}
```

📌 Resultado:

- 🔴 O **primeiro `<span>`** fica vermelho
    
- 🟢 O **primeiro `<p>`** fica verde
    

Mesmo eles **não sendo os primeiros filhos**!

---
