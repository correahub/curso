# **`:active`** é uma **pseudo-classe** que define **o estado de um elemento no momento em que ele está sendo ativado**, como durante um clique do mouse.



















---

# 🔘 O que é `:active`?

Imagine que um botão na página é como um **botão físico de brinquedo** 🎮.

- Quando o mouse passa por cima → `:hover` (ele percebe você) 👀
    
- **Quando você aperta o botão** → `:active` (ele sente o clique) 🔥
    

O `:active` é o momento **exato em que você está clicando**, ou seja, **enquanto o botão do mouse está pressionado**.

É como se o elemento dissesse:

> _“Você está me apertando agora!”_ 😲

---

# 🎨 Exemplo simples

```css
button:active {
  transform: scale(0.95);
}
```

**O que acontece?**  
O botão dá uma leve “afundada” enquanto você mantém o clique pressionado.

---

# 📌 Quando o `:active` acontece?

- no **momento do clique**
    
- quando o mouse está **pressionado**
    
- antes do clique ser solto
    

Ele dura **frações de segundo**, só enquanto você está segurando.

---

# 🧠 Definição simples e técnica

**`:active` é uma pseudo-class que aplica estilos no instante em que o elemento está sendo clicado ou pressionado.** 🎯✨🖱️

---

Se quiser, posso explicar agora a diferença entre `:hover`, `:focus` e `:active`. 😄🔥