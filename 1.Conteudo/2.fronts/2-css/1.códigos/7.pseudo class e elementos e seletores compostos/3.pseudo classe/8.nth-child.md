# **`:nth-child()`** é uma **pseudo-classe** que define **elementos com base na sua posição entre os filhos de um elemento pai**, permitindo selecionar padrões como números, pares ou ímpares.
















---

# 🔢 O que é `:nth-child()`?

Imagine que você tem uma **fila de crianças** dentro de uma caixa:

1️⃣ 👦  
2️⃣ 👧  
3️⃣ 👧  
4️⃣ 👦  
5️⃣ 👧

O `:nth-child()` serve para você escolher **qual criança da fila** quer estilizar:

- a 1ª
    
- a 2ª
    
- a 5ª
    
- todas as pares
    
- todas as ímpares
    
- de 3 em 3
    
- etc.
    

Ele é como um **controle remoto** para pegar posições específicas na fila 😄🎯.

---

# 🎨 Exemplos simples

## 👉 1. Escolher um filho exato

```css
li:nth-child(3) {
  color: red;
}
```

Só o **3º** `<li>` fica vermelho.

---

## 👉 2. Escolher todos os pares

```css
li:nth-child(even) {
  background: lightgray;
}
```

`even` = **pares** (2, 4, 6…)

---

## 👉 3. Escolher todos os ímpares

```css
li:nth-child(odd) {
  background: yellow;
}
```

`odd` = **ímpares** (1, 3, 5…)

---

## 👉 4. De 3 em 3

```css
li:nth-child(3n) {
  font-weight: bold;
}
```

Pega o 3º, 6º, 9º…

---

# ❗ Algo importante

O `:nth-child()` conta **todos os elementos**, independente do tipo.  
Ou seja: ele olha a posição **real no HTML**, não só os `<li>`, `<p>`, etc.

(Depois, se quiser, posso te ensinar `:nth-of-type()`, que olha _somente_ o tipo.)

---

**`:nth-child()` é uma pseudo-class que seleciona elementos pela sua posição na lista de filhos, podendo usar números fixos ou padrões (pares, ímpares, sequência com n).** 🎯✨🔢
