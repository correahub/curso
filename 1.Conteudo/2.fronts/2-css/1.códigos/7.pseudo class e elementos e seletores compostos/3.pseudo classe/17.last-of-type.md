# **`:last-of-type`** é uma **pseudo-classe** que define **o último elemento de um determinado tipo dentro de um elemento pai**, ignorando outros tipos de elementos.
























---

## O que é `:last-of-type`? 🧩

👉 **`:last-of-type` é um seletor do CSS** que escolhe **o ÚLTIMO elemento de um TIPO específico** dentro de um elemento pai.

📌 Ele **não liga para ser o último filho**  
📌 Ele liga apenas para ser **o último daquele tipo (tag)**

---

## Exemplo simples 👶✨

```html
<div>
  <p>Parágrafo 1</p>
  <p>Parágrafo 2</p>
  <span>Texto</span>
</div>
```

```css
p:last-of-type {
  color: red;
}
```

👉 Resultado:

- 🔴 **Parágrafo 2** fica vermelho
    
- O `<span>` é ignorado, porque **não é um `<p>`**
    

---

## Comparação importante ⚠️

### `:last-child` ❌ vs `:last-of-type` ✅

```html
<div>
  <p>Parágrafo 1</p>
  <p>Parágrafo 2</p>
  <span>Texto</span>
</div>
```

### ❌ `p:last-child`

```css
p:last-child {
  color: blue;
}
```

👉 **Não funciona**, porque o último filho é um `<span>`, não um `<p>`.

---

### ✅ `p:last-of-type`

```css
p:last-of-type {
  color: green;
}
```

👉 **Funciona**, porque ele pega **o último `<p>`**, mesmo não sendo o último filho.

---

## Como pensar nisso (modo criança 🧠🎈)

Imagine uma fila de brinquedos 🧸🚗🚗🎲:

- 🧸 Urso (`h1`)
    
- 🚗 Carrinho (`p`)
    
- 🚗 Carrinho (`p`)
    
- 🎲 Dado (`span`)
    

🔹 `:last-child` → pega **o último brinquedo da fila**  
🔹 `:last-of-type` → pega **o último brinquedo daquele tipo**

---

## Exemplo com vários tipos 👇

```html
<section>
  <h2>Título</h2>
  <p>Texto 1</p>
  <p>Texto 2</p>
  <p>Texto 3</p>
</section>
```

```css
p:last-of-type {
  font-weight: bold;
}
```

👉 Resultado:

- 🟢 **Texto 3** fica em negrito
    

---

## Definição técnica (simples, como você pediu 😄)

> **`:last-of-type` seleciona o último elemento de um determinado tipo (tag) dentro do seu elemento pai.** 🧠📄✨

---

Se quiser, posso explicar:

- `:first-of-type` vs `:last-of-type`
    
- `:nth-of-type()` com números
    
- exercícios rápidos pra fixar 💪😄