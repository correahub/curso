# **`:where()`** é uma **pseudo-classe** que define **elementos com base em um seletor, sem adicionar especificidade**, permitindo agrupar seletores sem afetar a prioridade dos estilos.

















---

## 📌 O que é `:where()` no CSS?

**`:where()` é uma pseudo-classe do CSS** que serve para **agrupar vários seletores em um só**, **igual ao `:is()`**, **MAS** com uma diferença MUITO importante:

> 🚨 **`:where()` tem especificidade ZERO**  
> Ele **não briga** com outros estilos 😌

---

## 🧒 Explicando como para uma criança

Imagine que o CSS é uma disputa de força 💪  
Algumas regras são fortes, outras são fracas.

👉 O `:where()` é como dizer:

> “Esse estilo é educado 🤝  
> Se alguém mais forte aparecer, eu deixo ele ganhar.”

---

## ❌ Sem `:where()` (repetição)

```css
h1, h2, h3 {
  margin: 0;
}
```

---

## ✅ Com `:where()` (organizado e fraco de propósito)

```css
:where(h1, h2, h3) {
  margin: 0;
}
```

✔ Código mais limpo  
✔ Fácil de manter  
✔ Não causa conflitos

---

## 🎯 O SEGREDO do `:where()`: especificidade ZERO 😮

Veja este exemplo:

```css
:where(h1) {
  color: red;
}

h1 {
  color: blue;
}
```

👉 Resultado final: **AZUL** 🔵  
Mesmo o `:where()` vindo antes ou depois, ele **sempre perde**.

Por quê?  
➡ Porque **a especificidade dele é 0**

---

## 📌 Comparando `:is()` e `:where()`

|Pseudo-classe|Especificidade|
|---|---|
|`:is()`|Usa a maior especificidade dos seletores|
|`:where()`|**Sempre 0** ❗|

Exemplo:

```css
:is(h1, .titulo) {
  color: green; /* força de classe */
}

:where(h1, .titulo) {
  color: red; /* força ZERO */
}
```

---

## 🧩 Onde o `:where()` é MUITO usado?

### 🔹 Reset CSS

```css
:where(h1, h2, p, ul, ol) {
  margin: 0;
  padding: 0;
}
```

### 🔹 Estilos base (design system)

```css
:where(button) {
  border: none;
  background: none;
}
```

### 🔹 Evitar conflitos em projetos grandes

Perfeito para bibliotecas, componentes e frameworks 🧩

---

## ⚠️ Regra de ouro

> 🟢 Use `:where()` quando você **NÃO quer brigar**  
> 🔴 Use `:is()` quando você **quer ganhar a disputa**

---

## 🧩 Definição técnica (como você pediu)

**`:where()` é uma pseudo-classe CSS que permite agrupar múltiplos seletores, aplicando estilos com especificidade zero, garantindo que essas regras nunca sobrescrevam regras mais específicas.** 

---

Se quiser, posso:

- mostrar exemplos reais de reset CSS
    
- explicar `:where()` + `:not()`
    
- fazer um resumo comparativo de todas as pseudo-classes modernas 😄📚