# **`:focus`** é uma **pseudo-classe** que define **o estado de um elemento quando ele está focado**, como ao clicar com o mouse ou navegar usando o teclado.






















---

# 🔍 O que é `:focus`?

Pense que cada elemento da página (como um input, botão, link) é como um **aluno na sala** 👦👧.

Quando você **clica** nesse aluno ou quando você **usa o teclado** para chegar até ele (como usando TAB), esse aluno fica **“atento”** e dizendo:

> _“Ei, agora sou eu que estou recebendo atenção!”_ 👀

O `:focus` é exatamente isso:  
Ele permite mudar o estilo de um elemento **quando ele está recebendo foco**, ou seja, quando está **pronto para digitar ou interagir**.

---

# 🎨 Exemplo simples

```css
input:focus {
  border: 2px solid blue;
}
```

**O que acontece?**  
Quando você clica no input ou seleciona ele com TAB, a borda fica azul porque ele está “focado”.

---



---

Se quiser, posso te mostrar exemplos com botão, link, textarea ou explicar a diferença entre `:focus` e `:focus-visible`. 😄🔥