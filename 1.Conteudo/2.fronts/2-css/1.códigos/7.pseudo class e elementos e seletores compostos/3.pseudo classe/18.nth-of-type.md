# **`:nth-of-type()`** é uma **pseudo-classe** que define **elementos com base na posição deles entre elementos do mesmo tipo**, permitindo selecionar padrões como números, pares ou ímpares;
























2 = PULA DE QUANTO EM QUANTO
n = CONTADOR
[ + =  começar no valor]
[ - = ignorar valores menores]
2 = Valor

3n +1 = começa no 1 e pinta de 3 em 3.
3n -1 =  começa no 2 e pinta de 3 em 3.

























---

## O que é `:nth-of-type()`? 🧩

👉 **`:nth-of-type()` é um seletor do CSS** que escolhe **um elemento pela POSIÇÃO dele dentro do seu TIPO (tag)**, dentro do elemento pai.

📌 Ele **ignora outros tipos de elementos**  
📌 Ele conta **só os elementos do mesmo tipo**

---

## Exemplo bem simples 👇

```html
<div>
  <p>Parágrafo 1</p>
  <span>Texto</span>
  <p>Parágrafo 2</p>
  <p>Parágrafo 3</p>
</div>
```

```css
p:nth-of-type(2) {
  color: red;
}
```

👉 Resultado:

- 🔴 **Parágrafo 2** fica vermelho
    
- O `<span>` não entra na conta, porque **não é `<p>`**
    

---

## Como pensar nisso (modo criança 🧠🎈)

Imagine brinquedos numa caixa 🎁:

- 🚗 Carrinho (`p`)
    
- 🧸 Urso (`span`)
    
- 🚗 Carrinho (`p`)
    
- 🚗 Carrinho (`p`)
    

🔹 `nth-of-type(2)` → pega **o segundo carrinho**, não o segundo brinquedo

---

## Valores mais usados do `nth-of-type()` 🔢

### 1️⃣ Número fixo

```css
p:nth-of-type(1) /* primeiro p */
p:nth-of-type(2) /* segundo p */
```

---

### 2️⃣ `odd` (ímpares) 🔵

```css
p:nth-of-type(odd) {
  background: lightgray;
}
```

👉 pega: 1º, 3º, 5º...

---

### 3️⃣ `even` (pares) 🟢

```css
p:nth-of-type(even) {
  background: lightblue;
}
```

👉 pega: 2º, 4º, 6º...

---

### 4️⃣ Fórmula matemática `an + b` 🧮

```css
p:nth-of-type(2n)   /* pares */
p:nth-of-type(2n+1) /* ímpares */
p:nth-of-type(3n)   /* 3º, 6º, 9º... */
```

📌 Explicação simples:

- `n` começa em 0
    
- Vai somando até achar posições válidas
    

---

## Comparação importante ⚠️

### `:nth-child()` ❌ vs `:nth-of-type()` ✅

```html
<div>
  <p>Parágrafo 1</p>
  <span>Texto</span>
  <p>Parágrafo 2</p>
</div>
```

```css
p:nth-child(2) {
  color: red;
}
```

❌ **Não funciona**, porque o segundo filho é um `<span>`.

```css
p:nth-of-type(2) {
  color: green;
}
```

✅ **Funciona**, porque é o **segundo `<p>`**, não o segundo filho.

---

## Definição técnica (do jeito que você gosta 😄)

> **`:nth-of-type()` seleciona elementos com base na posição que ocupam entre outros elementos do mesmo tipo dentro do mesmo pai.** 🧠📄✨

---

Se quiser, posso:

- te dar exercícios rápidos 📝
    
- explicar `nth-last-of-type()` ⏪
    
- montar um resumo com todos (`first`, `last`, `nth`) 😄