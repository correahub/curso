# **`:is()`** é uma **pseudo-classe** que define **elementos com base em um seletor, permitindo agrupar usando a especificidade de maior nível para todos.





















---

## 📌 O que é `:is()` no CSS?

**`:is()` é uma pseudo-classe do CSS** que serve para **agrupar vários seletores diferentes em um só**, deixando o código **menor, mais limpo e mais fácil de ler**.

👉 Pense assim:

> “Quero aplicar o mesmo estilo para _vários tipos de elementos_ sem repetir tudo.”

---

## 🧒 Explicando como para uma criança

Imagine que você quer pintar **maçãs, bananas e laranjas** de vermelho 🍎🍌🍊  
Em vez de pintar uma por uma, você diz:

> “Pinte **todas as frutas** de uma vez!”

O `:is()` faz exatamente isso no CSS 🎨

---

## ❌ Sem `:is()` (repetitivo)

```css
h1:hover,
h2:hover,
h3:hover {
  color: red;
}
```

Aqui você repete muita coisa 😵‍💫

---

## ✅ Com `:is()` (mais simples)

```css
:is(h1, h2, h3):hover {
  color: red;
}
```

✨ Mesmo resultado  
✨ Menos código  
✨ Mais fácil de entender

---

## 📌 Onde o `:is()` pode ser usado?

Ele pode agrupar **qualquer seletor**:

### 🔹 Elementos

```css
:is(p, span, a) {
  color: blue;
}
```

### 🔹 Classes

```css
:is(.btn, .link, .menu-item) {
  font-weight: bold;
}
```

### 🔹 Combinado com outros seletores

```css
section :is(h1, h2) {
  margin-bottom: 10px;
}
```

👉 Aqui:  
✔ Dentro de `section`  
✔ Pega `h1` **OU** `h2`

---

## 🎯 `:is()` e especificidade (⚠️ IMPORTANTE)

A **especificidade do `:is()` é a do seletor MAIS FORTE dentro dele**.

Exemplo:

```css
:is(h1, .titulo) {
  color: green;
}
```

➡ `.titulo` é mais forte que `h1`  
➡ Então o `:is()` terá força de **classe**

⚠️ Ele **não soma** especificidade, só pega a maior.

---

## 🤔 Diferença rápida: `:is()` vs `:where()`

|Pseudo-classe|Especificidade|
|---|---|
|`:is()`|Tem especificidade|
|`:where()`|Sempre ZERO 😮|

👉 Use `:is()` quando você **quer que o CSS tenha força**  
👉 Use `:where()` quando você **não quer brigar com outros estilos**

---

## 🧩 Definição técnica (como você pediu)

**`:is()` é uma pseudo-classe CSS que permite agrupar múltiplos seletores em um único seletor, aplicando estilos a qualquer elemento que corresponda a um dos seletores listados, usando a maior especificidade entre eles.** ✅✨

---

Se quiser, posso:

- comparar `:is()` com `:not()`
    
- mostrar exemplos reais de layout
    
- explicar com desenhos mentais ainda mais simples 😄🎨