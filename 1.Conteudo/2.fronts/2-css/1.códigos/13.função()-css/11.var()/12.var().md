# **`var()`** é uma função do CSS usada para **inserir o valor de uma variável CSS`.

Exemplo:

```css
:root {
  --cor-principal: #3498db;
}

div {
  background-color: var(--cor-principal);
}
```

Aqui, o `div` usa a cor definida na variável `--cor-principal`.
