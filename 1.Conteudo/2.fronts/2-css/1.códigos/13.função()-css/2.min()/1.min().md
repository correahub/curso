# **`min()`** é uma **função CSS** usada para **retornar o menor valor entre os valores fornecidos**.

Exemplo:

```css
div {
  width: min(50%, 200px); /* largura será o menor valor: 50% ou 200px */
}
```
