# **`rgb()`** é uma função do CSS usada para **definir cores combinando os valores de vermelho, verde e azul**.

Exemplo:

```css
div {
  background-color: rgb(255, 0, 0); /* vermelho */
}
```

Aqui, os valores vão de **0 a 255** para cada cor (R, G, B).
