# **`brightness()`** é uma função do CSS usada para **ajustar o brilho de um elemento ou imagem**.

Exemplo:

```css
img {
  filter: brightness(150%);
}
```

Aqui, a imagem ficará **50% mais clara** do que o original.