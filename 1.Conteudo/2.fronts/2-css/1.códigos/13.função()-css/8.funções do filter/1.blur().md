# **`blur()`** é uma função do CSS usada para **aplicar um Esfumaçamento a um elemento ou imagem**.

Exemplo:

```css
img {
  filter: blur(5px);
}
```

Aqui, a imagem ficará **desfocada em 5 pixels**, criando um efeito de suavização.
