# circle é o valor da função radial gradiente que  define o formato de um círculo perfeito, com **largura e altura iguais**, independentemente do tamanho do elemento.






















## 🔵 O que é `circle` no `radial-gradient`?

`circle` define que o gradiente radial terá **formato de círculo perfeito**, com **largura e altura iguais**, independentemente do tamanho do elemento.

```css
background: radial-gradient(circle, red, blue);
```

➡️ A transição de cores acontece **em todas as direções igualmente**, partindo do centro.

---

## 🧠 Sem `circle`, o que acontece?

Se você **não** usar `circle`, o padrão é `ellipse`:

```css
background: radial-gradient(red, blue);
```

Isso gera um gradiente **oval**, que se adapta ao formato do elemento.

📌 Exemplo:

- `div` larga → gradiente achatado
    
- `div` alta → gradiente esticado
    

---

## 📐 Como o tamanho do `circle` é calculado?

O círculo cresce até alcançar:

- `farthest-corner` (padrão)
    
- ou o valor que você definir
    

Exemplo:

```css
background: radial-gradient(circle, red, blue);
```

➡️ O raio do círculo vai até o **canto mais distante** do elemento.

---

## 📍 `circle` + posição (`at`)

```css
background: radial-gradient(circle at top left, red, blue);
```

➡️ O **centro do círculo** fica no canto superior esquerdo.

Outros exemplos:

```css
circle at center
circle at right
circle at bottom
```

---

## 📏 `circle` + tamanho

Você pode controlar o tamanho do círculo:

```css
background: radial-gradient(circle closest-side, red, blue);
background: radial-gradient(circle farthest-corner, red, blue);
```

Ou com medidas exatas:

```css
background: radial-gradient(circle 100px, red, blue);
```

➡️ Aqui o círculo terá **raio de 100px**.

---

## 🎨 `circle` + stops de cor

```css
background: radial-gradient(
  circle,
  #29d4fd 0%,
  #29d4fd 20%,
  #f95d08 80%,
  #000 100%
);
```

➡️ As porcentagens representam a **distância do centro até a borda do círculo**.

---

## 🧪 Quando usar `circle`?

Use `circle` quando você quiser:

- brilho central ✨
    
- efeito de luz
    
- aura
    
- explosão
    
- foco visual perfeito
    

Evite se quiser que o gradiente **se adapte ao formato do elemento** (use `ellipse`).

---

## 🧠 Resumo ultra rápido

- `circle` = gradiente **100% circular**
    
- Não depende do formato do elemento
    
- Pode ter posição, tamanho e cores controladas
    
- Ideal para efeitos visuais fortes
    

Se quiser, posso explicar **`ellipse` com o mesmo nível de detalhe**, ou mostrar **casos reais de UI** (botões, cards, backgrounds).