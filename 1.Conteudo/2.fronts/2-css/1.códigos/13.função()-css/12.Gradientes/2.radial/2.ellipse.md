# `ellipse` é o valor da função radial gradiente que define o **formato oval (elíptico)**, ou seja, **largura e altura diferentes**, adaptando-se ao tamanho do elemento.



















---

## 🟣 O que é `ellipse` no `radial-gradient`?

`ellipse` define que o gradiente radial terá **formato oval (elíptico)**, ou seja, **largura e altura diferentes**, adaptando-se ao tamanho do elemento.

👉 **Esse é o comportamento padrão** do `radial-gradient`.

```css
background: radial-gradient(ellipse, red, blue);
```

➡️ A cor se espalha **mais em uma direção do que na outra**, dependendo do formato do elemento.

---

## 🧠 O que acontece sem escrever `ellipse`?

Nada muda 😄  
Isso aqui:

```css
background: radial-gradient(red, blue);
```

É exatamente o mesmo que:

```css
background: radial-gradient(ellipse, red, blue);
```

---

## 📐 Como o tamanho do `ellipse` é calculado?

A elipse cresce até alcançar o limite definido pelo tamanho:

```css
background: radial-gradient(ellipse farthest-corner, red, blue);
```

Padrão:

- eixo horizontal → acompanha a largura
    
- eixo vertical → acompanha a altura
    

Por isso o gradiente “estica”.

---

## 📍 `ellipse` + posição (`at`)

```css
background: radial-gradient(ellipse at top, red, blue);
```

➡️ O centro da elipse fica no topo do elemento.

Outros exemplos:

```css
ellipse at center
ellipse at left
ellipse at bottom right
```

---

## 📏 `ellipse` + tamanho

Você pode controlar **os dois raios** da elipse:

```css
background: radial-gradient(ellipse 150px 80px, red, blue);
```

➡️

- `150px` → raio horizontal
    
- `80px` → raio vertical
    

Ou com palavras-chave:

```css
ellipse closest-side
ellipse farthest-side
ellipse closest-corner
ellipse farthest-corner
```

---

## 🎨 `ellipse` + stops de cor

```css
background: radial-gradient(
  ellipse,
  #29d4fd 0%,
  #29d4fd 25%,
  #f95d08 70%,
  #000 100%
);
```

➡️ As porcentagens seguem o **contorno da elipse**, não um círculo.

Isso cria efeitos mais naturais em:

- telas widescreen
    
- cards largos
    
- headers
    
- fundos de página
    

---

## 🔥 Quando usar `ellipse`?

Use `ellipse` quando:

- o elemento **não é quadrado**
    
- você quer um gradiente mais suave e natural
    
- o fundo precisa “abraçar” o layout
    
- criar iluminação ambiente
    

Use `circle` quando quiser foco visual perfeito.

---

## 🧠 Resumo ultra rápido

- `ellipse` = gradiente **oval**
    
- É o **padrão** do `radial-gradient`
    
- Se adapta ao tamanho do elemento
    
- Permite controle independente de largura e altura
    
- Ideal para backgrounds e iluminação suave
    
