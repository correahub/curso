# **`inherit`** é o **valor** que faz um atributo **herdar o valor do elemento pai**, copiando exatamente o que ele usa.

























---

## 📌 O que é `inherit`?

**`inherit` é um valor que diz:  
“Ei, pega o valor do elemento pai e usa igualzinho aqui.”**

👉 Ele **força a herança** de uma propriedade.

---

## 👨‍👦 Ideia bem simples

Imagine assim:

- O **pai** tem uma cor 🎨
    
- O **filho** normalmente não copia essa cor
    
- Mas se o filho usar `inherit` 👉 ele copia **exatamente** a cor do pai
    

---

## 🧠 Definição técnica (bem simples)

**`inherit` faz uma propriedade usar o mesmo valor do elemento pai, mesmo que essa propriedade normalmente NÃO seja herdada.** 🧬✨

---

## ✍️ Exemplo prático

```css
div {
  border: 2px solid red;
}

p {
  border: inherit;
}
```

🔹 O que acontece:

- O `div` tem borda vermelha
    
- O `p` **não herda borda por padrão**
    
- Mas com `inherit` 👉 o `p` copia a borda do `div`
    

✅ Resultado: **o `p` também fica com borda vermelha**

---

## 📎 Importante entender isso

### ❌ Nem toda propriedade herda automaticamente

Exemplos que **NÃO herdam**:

- `margin`
    
- `padding`
    
- `border`
    
- `width`
    
- `height`
    

👉 Com `inherit`, elas **passam a herdar**.

---

### ✅ Propriedades que já herdam sozinhas

Exemplos:

- `color`
    
- `font-size`
    
- `font-family`
    

Mesmo assim, você **pode usar `inherit`** se quiser deixar explícito.

---

## 🧪 Outro exemplo simples

```css
body {
  color: blue;
}

span {
  color: inherit;
}
```

📘 O `span` fica **azul**, igual ao `body`.

---

## 🧩 Quando usar `inherit`?

Use quando você quer:

- Garantir que o filho copie o pai
    
- Forçar herança onde não existe
    
- Criar estilos consistentes
    

---

## 🧠 Frase para memorizar

👉 **`inherit` = copiar exatamente o valor do pai** 👨‍👦✨

---

## 🧾 Definição curta (do jeito que você gosta)

✨ Quer que eu explique agora a diferença entre **`inherit`**, **`initial`**, **`unset`** e **`revert`** lado a lado?