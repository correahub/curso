# **`background-color`** é o atributo que define a **cor de fundo de um elemento**.


























## 🧠 O que é `background-color`?

A propriedade `background-color` do CSS **define a cor de fundo** de um elemento da página — como um parágrafo, um título, uma caixa, etc.

Pense assim: se o elemento fosse uma folha de papel, o `background-color` é como pintar o verso dela com alguma cor.

---

## 📌 Como se usa?

Você escreve `background-color` dentro de uma **regra de estilo CSS** para dizer qual cor quer usar no fundo daquele elemento.

---

## 🧪 Exemplo prático

Vamos pintar de amarelo o fundo de um parágrafo:

### 🧾 Código HTML + CSS:

```html
<!DOCTYPE html>
<html>
<head>
  <style>
    p {
      background-color: yellow;
    }
  </style>
</head>
<body>

  <p>Este é um parágrafo com fundo amarelo!</p>

</body>
</html>
```

### 🔍 Explicando o que cada parte faz:

- `<style>...</style>`: é onde colocamos o CSS.
    
- `p`: é o seletor que diz “eu quero aplicar o estilo a todos os parágrafos (`<p>`)”.
    
- `background-color: yellow;`: está dizendo “pinte o fundo do parágrafo de **amarelo**”.
    

---

## 🎨 Dicas de cores que você pode usar:

Você pode escrever a cor de várias formas:

- **Nome da cor**: `red`, `blue`, `green`, `yellow`...
    
- **Código hexadecimal**: `#ff0000` (vermelho), `#00ff00` (verde)...
    
- **RGB**: `rgb(255, 255, 0)` (amarelo)
    

---

## ✅ Resumo:

- `background-color` serve para **mudar a cor de fundo** de qualquer elemento.
    
- Pode usar nomes, códigos ou valores RGB.
    
- Muito útil para **destacar partes** do seu site.
    

Se quiser, posso te mostrar um exemplo com clique ou interação também!