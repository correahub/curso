# background-blend-mode é um atributo que **define como as camadas de fundo de um elemento são misturadas entre si.



















---

### 🔹 Com o valor`normal`**Não há mistura**: o fundo de cima cobre o de baixo (padrão).

```css
background-blend-mode: normal;
```

---













### 🔹 Com o valor `multiply` **Multiplica as cores**, deixando o resultado mais escuro.

```css
background-blend-mode: multiply;
```

---
















### 🔹Com o valor `screen` **Clareia a mistura**, efeito oposto ao `multiply`.

```css
background-blend-mode: screen;
```

---














### Com o valor🔹 `overlay` **Combina `multiply` e `screen`**, aumentando contraste.

```css
background-blend-mode: overlay;
```

---














### 🔹 Com o valor `darken` **Mantém a cor mais escura** entre as camadas.

```css
background-blend-mode: darken;
```

---














### 🔹Com o valor `lighten` **Mantém a cor mais clara** entre as camadas.

```css
background-blend-mode: lighten;
```

---








### 🔹 Com o valor `color` **Usa a cor de cima e preserva a luminosidade de baixo**.

```css
background-blend-mode: color;
```

---















### 🔹Com o valor `luminosity` **Usa a luminosidade de cima e a cor de baixo**.

```css
background-blend-mode: luminosity;
```
