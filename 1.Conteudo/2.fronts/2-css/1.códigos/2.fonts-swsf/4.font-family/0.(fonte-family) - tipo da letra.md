# **`font-family`** é o **atributo** que define **a família da fonte usada no texto**, como Arial, Times New Roman ou fontes personalizadas.


























## 📝 O que é `font-family`?

A propriedade `font-family` no CSS serve para **definir a fonte do texto** que aparece em uma página da web.

> Fonte (ou "font" em inglês) é o estilo das letras, como Arial, Times New Roman, ou Comic Sans.

---

## 🧠 Como funciona?

Você informa **quais fontes o navegador deve tentar usar**, na ordem de preferência. Se a primeira fonte não estiver disponível no computador da pessoa que acessa o site, ele tenta a próxima, e assim por diante.

### 🧱 Estrutura básica:

```css
font-family: Fonte1, Fonte2, FonteGenérica;
```

- As fontes devem ser separadas por vírgulas.
    
- Se o nome da fonte tiver **espaço**, você precisa colocar entre **aspas**.
    
- A **última opção** geralmente é um **tipo genérico**, como:
    
    - `serif` (com serifa),
        
    - `sans-serif` (sem serifa),
        
    - `monospace` (tipo máquina de escrever).
        

---

## ✅ Exemplo prático:

Imagine que você quer que o texto do seu site apareça na fonte **Arial**, mas se não tiver Arial, use **Helvetica**. Se nenhuma delas estiver disponível, use qualquer fonte **sem serifa** (limpa, moderna).

```html
<!DOCTYPE html>
<html>
<head>
  <style>
    p {
      font-family: Arial, Helvetica, sans-serif;
    }
  </style>
</head>
<body>
  <p>Olá! Este texto está usando a fonte Arial (ou uma parecida).</p>
</body>
</html>
```

### O que acontece aqui:

1. O navegador tenta usar **Arial**.
    
2. Se o computador da pessoa não tiver Arial, tenta **Helvetica**.
    
3. Se também não tiver Helvetica, usa qualquer fonte do tipo **sans-serif** disponível.
    

---

## 📌 Dicas extras:

- Sempre inclua uma fonte **genérica** no final (como `sans-serif`), para garantir que algo parecido seja usado.
    
- Você pode usar fontes personalizadas (como do Google Fonts), mas isso envolve um passo a mais — por enquanto, foque nas fontes básicas.
    

---

Se quiser, posso te mostrar como usar uma fonte do Google Fonts em seguida. Quer tentar isso?