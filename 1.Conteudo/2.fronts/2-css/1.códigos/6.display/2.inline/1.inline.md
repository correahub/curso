# **`inline`** é um **valor** do atributo `display` que define que **o elemento será exibido na mesma linha que outros elementos**, ocupando apenas o espaço do seu próprio conteúdo, sem aceitar valores de altura e largura.
























🧒💬 "Tio, e o que é um elemento **inline**?"

Ótima pergunta!  
Se o **bloco** era como um **bloco de montar grandão**, o **inline** é como **pecinhas pequenas** que ficam **uma do lado da outra**!

---

### 🎨 Vamos imaginar lápis de cor:

```html
<span>🖍️</span><span>🖍️</span><span>🖍️</span>
```

Esses lápis **ficam todos na mesma linha**.  
Eles não ocupam a linha inteira. Só o espaço necessário pra eles.

---

## 📦 O que é um elemento inline?

Um elemento **inline** é um tipo de caixinha **pequenininha** que **fica na mesma linha** que os outros.

Ele **só ocupa o espaço que precisa** e **não quebra a linha**!

---

### 🧠 Exemplos de elementos inline:

- `<span>`
    
- `<a>`
    
- `<strong>`
    
- `<em>`
    
- `<img>`  
    (E vários outros que ficam "dentro do texto")
    

---

### 🖼️ Imagina assim:

```html
<p>Eu gosto de <span>pizza</span> e <span>sorvete</span>!</p>
```

Vai aparecer assim:

```
Eu gosto de pizza e sorvete!
```

Os `<span>` **ficam no meio da frase**, como se fossem parte do texto.

---

### 🆚 Bloco vs Inline

|Tipo|Ocupa a linha toda?|Fica um embaixo do outro?|Pode ficar no meio do texto?|
|---|---|---|---|
|**Bloco**|Sim|Sim|Não|
|**Inline**|Não|Não|Sim!|

---

## 🧩 Resumindo com uma frase simples:

> "Elemento inline é uma caixinha que **não ocupa a linha toda**.  
> Ela gosta de ficar **do ladinho dos outros** como se fosse **parte de uma frase**!"

---
