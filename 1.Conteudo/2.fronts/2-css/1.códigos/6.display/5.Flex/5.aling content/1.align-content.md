 um **valor** do atributo `flex-wrap` que define **que 
# `align-content`** é o **atributo** que define **como as linhas de um contêiner flexível são distribuídas ao longo do eixo secundário**, quando o wrap acontece.






















----

# 🎨 O que é **align-content**?

Imagine que você tem **várias fileiras de brinquedos** no chão. Cada fileira é um “grupo” de brinquedos.

Agora imagine que esse chão é **maior** que o espaço que essas fileiras ocupam.  
A pergunta é: _“Como eu organizo o espaço vazio **entre** essas fileiras?”_

👉 **align-content** faz exatamente isso no CSS:  
Ele **organiza o espaço entre as linhas** do flex container **quando existem várias linhas** (ou seja, quando `flex-wrap: wrap` está ativado).

---

# 🧠 Explicação infantil (mas detalhada)

Pensa assim:

- Um container flex que quebra linhas (`flex-wrap: wrap`) cria **várias fileiras** de itens.
    
- **align-content** não mexe nos itens individualmente.
    
- Ele mexe **nas linhas** inteiras, decidindo:
    
    - se elas ficam encostadas no topo,
        
    - no fundo,
        
    - no meio,
        
    - espalhadas igualmente,
        
    - ou esticadas para preencher todo espaço.
        

📌 MUITO IMPORTANTE:  
Se existir **só uma linha**, align-content **não faz nada** (porque não há linhas para distribuir). É como tentar organizar espaço entre “fileiras” quando só existe uma.

---

# 🎯 Exemplo visual (sem código)

Imagine várias linhas assim:

```
[🧸🧸🧸]
[🚗🚗🚗]
[🪀🪀🪀]
```

O container é maior que o conteúdo.  
O **align-content** vai decidir como essas 3 linhas ficam no espaço disponível.

---

# 🧪 Exemplo simples em código

```html
<div class="caixa">
  <div>A</div>
  <div>B</div>
  <div>C</div>
  <div>D</div>
  <div>E</div>
  <div>F</div>
</div>
```

```css
.caixa {
  display: flex;
  flex-wrap: wrap;
  height: 300px;
  align-content: space-between; /* muda aqui */
}
.caixa > div {
  width: 100px;
  height: 50px;
  background: lightblue;
  margin: 5px;
}
```

`align-content: space-between;`  
👉 Vai colocar **espaço igual entre as linhas**, mas **sem espaço nas bordas** (elas ficam coladas no topo e no fundo).

---

