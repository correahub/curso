# ****`flex-basis`** é o **atributo** que define **o tamanho inicial de um item flexível** antes da distribuição do espaço no contêiner flex.





















---

# 🎁 O que é **flex-basis**?

**flex-basis** é a propriedade que diz **qual deve ser o tamanho inicial de um item flex** antes do Flexbox começar a distribuir espaço.  
Pensa como:  
👉 “Eu quero começar com este tamanho aqui!”

Depois disso, o **flex-grow** e o **flex-shrink** decidem se o item vai crescer ou encolher.

---

# 🧸 Explicando como para uma criança

Imagine que você tem três caixinhas, e vai colocá-las dentro de uma caixa grande.

- O **flex-basis** é como se você dissesse:  
    **"Caixinha, eu quero que você comece com 100px de largura!"** 📦➡️📏
    

Aí o navegador tenta respeitar esse tamanho quando organiza tudo.

Se tiver espaço sobrando ou faltando, os outros poderes (grow e shrink) entram em ação, mas **flex-basis é sempre o tamanho inicial**.


---

# 🤓 Exemplinho rápido de código

```css
.item {
  flex-basis: 150px;
}
```

Isso quer dizer:  
➡️ "Esse item deve começar com **150px** no eixo principal."

---

# 📌 Dicas importantes

### ✔️ 1. Ele depende do **eixo principal**

Se o `flex-direction` for **row**, flex-basis controla **largura**.  
Se for **column**, controla **altura**.

### ✔️ 2. Ele substitui o width/height

Se você usar `flex-basis` e `width` juntos em `flex-direction: row`,  
o navegador **vai obedecer o flex-basis**.

### ✔️ 3. Você pode usar valores como:

- `auto` (padrão — usa width/height)
    
- `0` (começa do zero)
    
- `100px`
    
- `30%`
    

---

# 🎨 Exemplo visual simples

```css
.container {
  display: flex;
}

.item {
  flex-basis: 200px; /* Cada item começa com 200px */
}
```

Cada item tenta iniciar com **200px**.  
Se der espaço — ótimo!  
Se não der — o flexbox ajusta.

---
