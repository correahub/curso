# **`flex`** é o **atributo** que define **de forma abreviada como um item flexível cresce, encolhe e qual é seu tamanho base**, combinando `flex-grow`, `flex-shrink` e `flex-basis`. 

















---

# 🌟 O que é a propriedade `flex`?

A propriedade **`flex`** é uma forma **abreviada** (shorthand) de escrever **três propriedades juntas**:

- `flex-grow` 🟩 (crescer)
    
- `flex-shrink` 🟥 (encolher)
    
- `flex-basis` 🟦 (tamanho inicial)
    

Ou seja:  
👉 **`flex` = grow shrink basis`**

É como colocar **três coisas dentro de uma caixinha só**, para ficar mais rápido de escrever.

---

# 👶 Explicando como se fosse para uma criança

Imagine que você tem **três crianças** brincando dentro de uma sala.  
Cada criança é um **item flex**.

Agora vamos usar as três propriedades:

---

## 🟦 1. flex-basis — “Qual o tamanho que você quer começar?”

É como perguntar para a criança:

> “Qual tamanho de espaço você quer ocupar _antes_ da brincadeira começar?”

É o **tamanho inicial**.

---

## 🟩 2. flex-grow — “Se sobrar espaço, você quer crescer?”

Pergunta para a criança:

> “Se a sala for grande e sobrar espaço, você quer pegar mais espaço?”

Se `flex-grow: 1`, ela **cresce**.  
Se `0`, ela **não cresce**.

---

## 🟥 3. flex-shrink — “Se faltar espaço, você aceita encolher?”

Agora você pergunta:

> “Se a sala ficar apertada, você aceita diminuir um pouquinho?”

Se `1`, ela **encolhe**.  
Se `0`, ela **não encolhe nunca**.

---

# 🌟 Como fica usando só `flex`?

### ✔️ Exemplo:

```css
.item {
  flex: 1 2 100px;
}
```

Aqui significa:

- `flex-grow: 1` 🟩 (pode crescer um pouco)
    
- `flex-shrink: 2` 🟥 (pode encolher bastante)
    
- `flex-basis: 100px` 🟦 (começa valendo 100px)
    

---

# ⭐ Os valores mais usados

## 👉 `flex: 1`

Significa:

- `grow: 1`
    
- `shrink: 1`
    
- `basis: 0`
    

É o mais comum para fazer itens ocuparem o mesmo tamanho.

## 👉 `flex: 0`

Significa:

- `grow: 0`
    
- `shrink: 1`
    
- `basis: 0`
    

Ele **não cresce**, mas pode encolher.

## 👉 `flex: none`

Significa:

- `grow: 0`
    
- `shrink: 0`
    
- `basis: auto`
    

Ele **não cresce**, **não encolhe**, fica do tamanho natural dele.

## 👉 `flex: auto`

Significa:

- `grow: 1`
    
- `shrink: 1`
    
- `basis: auto`
    

Ele cresce e encolhe, mas começa do tamanho "natural" do conteúdo.

---

# 📌 Definição simples (como você gosta) 😄

> **`flex` é a propriedade que combina o tamanho inicial, a capacidade de crescer e a capacidade de encolher do item flex, usando a ordem: grow → shrink → basis.**

---
