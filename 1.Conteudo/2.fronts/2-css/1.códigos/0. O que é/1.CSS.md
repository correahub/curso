 # CSS é uma linguagem de estilo usada para definir o estilo visual dos elementos HTML.
Elemento > Declaração > atributo > valor














---

## 🎨 O que é CSS?

**CSS** é como a **roupa** de um site 👕👗

Imagine assim 👇

- 🧱 **HTML** → é o **corpo da pessoa** (estrutura: cabeça, braços, pernas)
    
- 🎨 **CSS** → é a **roupa, cor do cabelo, tamanho, posição e estilo**
    

Sem CSS, o site existe…  
👉 mas fica **feio, sem cor, sem organização** 😵

---

## 🧠 Para que o CSS serve?

O CSS serve para **controlar a aparência dos elementos** da página, como:

- 🎨 **Cores** (texto, fundo, bordas)
    
- 🔠 **Tamanho da letra**
    
- 📏 **Espaçamentos** (margem e padding)
    
- 📐 **Tamanho dos elementos**
    
- 📍 **Posição** na tela
    
- 🎞️ **Animações e transições**
    
- 📱 **Responsividade** (funcionar bem no celular)
    

---

## 🧩 Exemplo simples

### HTML (estrutura)

```html
<p>Olá mundo</p>
```

### CSS (estilo)

```css
p {
  color: red;
  font-size: 20px;
}
```

📌 Resultado:  
O texto **“Olá mundo”** fica **vermelho** 🔴 e **grande** 🔠

---

## 🏗️ Como o CSS funciona?

O CSS funciona com **regras**, e cada regra tem 3 partes:

```css
seletor {
  propriedade: valor;
}
```

### Exemplo explicado como criança 👶

```css
p {
  color: blue;
}
```

- 🎯 **p** → quem eu quero mudar (o alvo)
    
- 🎨 **color** → o que eu quero mudar
    
- 🔵 **blue** → como eu quero que fique
    

---

## 📦 Onde o CSS pode ser usado?

1️⃣ **Dentro da tag HTML** (não recomendado 😬)  
2️⃣ **Dentro da tag `<style>`**  
3️⃣ **Em um arquivo separado `.css`** ✅ (forma correta)

---

## 🧠 Resumão bem curto

- 🧱 HTML = estrutura
    
- 🎨 CSS = aparência
    
- 🧠 CSS deixa o site bonito, organizado e animado
    

---

## 📘 Definição técnica (simples)

> **CSS (Cascading Style Sheets)** é uma linguagem usada para definir o estilo visual e o layout dos elementos HTML em uma página web.

Se quiser, posso explicar:

- 📌 como o CSS “conversa” com o HTML
    
- 📌 o que significa “cascata”
    
- 📌 seletores, classes e ids
    

Só me dizer 😄