# **`pre`** é um elemento usado para **exibir texto pré-formatado**, preservando espaços e quebras de linha.

Exemplo:

```html
<pre>
Linha 1
    Linha 2 com espaços
Linha 3
</pre>
```

<pre>
Linha 1
	    Linha 2 com espaços
Linha 3
</pre>
