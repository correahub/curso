# **`abbr`** é um elemento usado para **representar uma abreviação ou sigla**.

Exemplo:

```html
<p><abbr title="HyperText Markup Language">HTML</abbr> é usado na web.</p>
```

**Atributo:**

- **`title`**: mostra o significado completo da abreviação ao passar o mouse

