# **`dfn`** é um elemento usado para **definir um termo que aparece pela primeira vez no texto**.

Exemplo:

```html
<p><dfn title="Linguagem de marcação de hipertexto">HTML</dfn> é a linguagem de marcação da web.</p>
```

<p><dfn>HTML</dfn> é a linguagem de marcação da web.</p>
