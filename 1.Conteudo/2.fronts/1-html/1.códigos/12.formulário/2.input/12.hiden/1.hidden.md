
# O `input type="hidden"` é um campo de entrada **invisível ao usuário**, usado para **enviar dados junto com um formulário** sem aparecer na tela.

**Exemplo em HTML:**

```html
<form action="/enviar" method="post">
  <input type="hidden" name="usuarioId" value="123">
  <button type="submit">Enviar</button>
</form>
```

👉 Nesse exemplo, o valor `usuarioId=123` é enviado para o servidor, mesmo sem o usuário ver ou editar o campo.