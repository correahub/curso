# **`option`** é um elemento usado para **definir uma opção dentro de um `<select>`**.

Exemplo:

```html
<select name="fruta">
  <option value="maca">Maçã</option>
  <option value="banana">Banana</option>
  <option value="laranja">Laranja</option>
</select>
```
