## 🔀 `git checkout`

`git checkout` é um comando usado para **trocar de branch** ou **voltar para um commit específico**.

git checkout log e nome de arquivo para recuperar arquivo deletado, que só existe naquela versão

---

## 🌿 Trocar de branch

```bash
git checkout minha-branch
```

Isso muda você para a branch `minha-branch`.

---

## 🆕 Criar e trocar para uma nova branch

```bash
git checkout -b nova-branch
```

- `-b` → cria a branch
    
- Depois já troca para ela automaticamente
    

---

## ⏪ Voltar para um commit específico

```bash
git checkout abc1234
```

(onde `abc1234` é o início do hash do commit)

⚠️ Nesse caso você entra em **detached HEAD** (modo separado da branch).

---

## 🧠 Em resumo:

> `git checkout` serve para navegar entre branches ou commits.

📌 Observação: Hoje em dia, o Git recomenda usar:

- `git switch` → para branches
    
- `git restore` → para arquivos
    

Mas `checkout` ainda é muito usado 🚀