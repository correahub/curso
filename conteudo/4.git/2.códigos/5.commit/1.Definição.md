# `git commit` é o comando que salva definitivamente as alterações no histórico do Git.

























## 📦 `git commit`

# `git commit` é o comando que salva definitivamente as alterações no histórico do Git.

Ele cria uma “foto” do projeto naquele momento 📸

---

### 🔧 Exemplo:

```bash
git commit -m "Adiciona página inicial"
```

- `-m` → permite escrever a mensagem do commit
    
- A mensagem explica o que foi feito
    

---

### 🧠 O que acontece?

Depois de usar:

```bash
git add .
git commit -m "Mensagem"
```

O Git:

- 🔒 Registra as mudanças
    
- 🕒 Salva no histórico
    
- 👤 Associa ao seu `user.name` e `user.email`
    

---

### 🧠 Em resumo:

> `git commit` salva as alterações preparadas no stage no histórico do projeto.

Se quiser, posso explicar agora o `git log` 📜