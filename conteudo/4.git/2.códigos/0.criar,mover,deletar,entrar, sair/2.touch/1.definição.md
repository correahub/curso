# Touch é um comando usado no terminal para criar um novo arquivo vazio.






















## 📄 `touch`

# `touch` é um comando usado no terminal para **criar um novo arquivo vazio**.

---

### 🔧 Exemplo:

```bash
touch index.html
```

Isso cria um arquivo chamado `index.html` no diretório atual.

---

### 📌 Criando vários arquivos:

```bash
touch app.js style.css README.md
```

---

### 🧠 Em resumo:

> `touch` serve para criar arquivos rapidamente pelo terminal.

Se quiser, posso explicar agora o `rm` 😏