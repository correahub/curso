# Mkdir é um comando usado no terminal para **criar uma nova pasta**.




















## 📁 `mkdir`

`mkdir` significa **“make directory”** (criar diretório).

É um comando usado no terminal para **criar uma nova pasta**.

---

### 🔧 Exemplo:

```bash
mkdir projeto
```

Isso cria uma pasta chamada `projeto` no diretório atual.

---

### 📌 Criando várias pastas de uma vez:

```bash
mkdir pasta1 pasta2 pasta3
```

---

### 🧠 Em resumo:

> `mkdir` serve para criar pastas pelo terminal.

Se quiser, posso explicar agora o `cd` 😉