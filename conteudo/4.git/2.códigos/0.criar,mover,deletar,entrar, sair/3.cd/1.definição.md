# `Cd`serve para **entrar em uma pasta** pelo terminal.

# Cd .. serve para voltar uma pasta pelo terminal.






















## 📂 `cd`

# `cd` serve para **entrar em uma pasta** pelo terminal.

---

### 🔧 Exemplo:

```bash
cd projeto
```

Isso faz você entrar na pasta `projeto`.

---

## 🔙 `cd ..`

`cd ..` serve para **voltar uma pasta** (ir para o diretório pai).

---

### 🔧 Exemplo:

```bash
cd ..
```

Se você estiver em:

```
/home/user/projeto
```

Depois do `cd ..`, você vai para:

```
/home/user
```

---

### 🧠 Em resumo:

- `cd nome-da-pasta` → entra na pasta 📂
    
- `cd ..` → volta uma pasta 🔙
    

Se quiser, posso explicar também `cd /` e `cd ~` 😉