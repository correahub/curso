# git config user.name Define o nome do programador que aparecerá nos seus commits.




















# git config user.email` Define o email do autor que ficará associado aos seus commits.





















### 🔧 Exemplo:

```bash
git config --global user.name "João Silva"
```

---

# `git config user.email` Define o **email do autor** que ficará associado aos seus commits.

### 🔧 Exemplo:

```bash
git config --global user.email "joao@email.com"
```

---

## 🌍 O que significa `--global`?

- `--global` → vale para **todos os projetos** no seu computador
    
- Sem `--global` → vale **apenas para o projeto atual**
    

---

## 🧠 Em resumo:

> Esses comandos configuram sua identidade no Git.  
> Todo commit registra **nome + email** como assinatura.

Se quiser, posso explicar agora o `git config --list` 🔎