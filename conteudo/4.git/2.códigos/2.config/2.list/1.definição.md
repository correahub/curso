# `git config --list` é o comando que mostra todas as configurações atuais do Git.






















## 📋 `git config --list`

Ele exibe informações como:

- 👤 `user.name`
    
- 📧 `user.email`
    
- 🌿 branch padrão
    
- ⚙️ outras configurações ativas
    

---

### 🔧 Exemplo:

```bash
git config --list
```

---

### 🧠 O que ele faz?

Ele lista configurações vindas de:

- 📍 Configuração global (`--global`)
    
- 📁 Configuração local (do projeto atual)
    
- 🖥️ Configuração do sistema
    

---

### 🧠 Em resumo:

> `git config --list` serve para ver como o Git está configurado no momento.