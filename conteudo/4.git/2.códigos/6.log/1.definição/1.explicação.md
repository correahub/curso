# `git log` é o comando que **mostra o histórico de commits** do projeto.

---

### 🔧 Exemplo:

```bash
git log
```

---

### 🧠 O que ele exibe?

Para cada commit, ele mostra:

- 🔑 Hash (código único do commit)
    
- 👤 Autor
    
- 📧 Email
    
- 📅 Data
    
- 📝 Mensagem do commit
    

---

### 📌 Versão mais resumida:

```bash
git log --oneline
```

Mostra cada commit em uma linha (mais fácil de visualizar 👌).

---

### 🧠 Em resumo:

> `git log` permite ver todas as versões salvas do projeto.

Se quiser, posso explicar o `git diff` agora 🔥