## 🚀 `git push`

`git push` é o comando que **envia seus commits para um repositório remoto**.

Ou seja, ele pega o que está no seu Git local e manda para um servidor (como o GitHub).

---

## 🔧 Exemplo básico

```bash
git push
```

Envia os commits para o repositório remoto configurado.

---

## 🌿 Enviando uma branch específica

```bash
git push origin main
```

- `origin` → nome do repositório remoto
    
- `main` → nome da branch
    

---

## 🧠 O que acontece?

1. Você faz `git add`
    
2. Depois `git commit`
    
3. E por fim `git push`
    

Assim, suas alterações ficam salvas online 🌍

---

## 🧠 Em resumo:

> `git push` envia seus commits locais para o repositório remoto.

Se quiser, posso explicar agora o `git pull` 🔄