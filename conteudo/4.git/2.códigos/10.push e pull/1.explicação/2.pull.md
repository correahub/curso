## 🔄 `git pull`

`git pull` é o comando que **baixa as atualizações do repositório remoto e já as integra no seu projeto local**.

Ou seja, ele pega as mudanças que estão no servidor (como no GitHub) e atualiza sua branch.

---

## 🔧 Exemplo

```bash
git pull
```

---

## 🌿 Especificando remoto e branch

```bash
git pull origin main
```

- `origin` → repositório remoto
    
- `main` → branch
    

---

## 🧠 O que acontece por trás?

`git pull` é basicamente a junção de:

1. `git fetch` → baixa as mudanças
    
2. `git merge` → junta com sua branch atual
    

---

## 🧠 Em resumo:

> `git pull` atualiza seu projeto local com as mudanças do repositório remoto.

Se quiser, posso explicar agora o `git clone` 📥