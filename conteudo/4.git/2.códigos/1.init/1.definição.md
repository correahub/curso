# git init` é o comando que inicializa um repositório Git em uma pasta.























## 💾 `git init`

`git init` é o comando que **inicializa um repositório Git em uma pasta**.

Ou seja, ele transforma uma pasta comum em um projeto controlado pelo Git 📂✨

---

### 🔧 O que ele faz?

Quando você executa:

```bash
git init
```

O Git cria uma pasta oculta chamada:

```
.git
```

Essa pasta guarda **todo o histórico, commits, branches e configurações** do projeto.

---

### 🧠 Em resumo:

> `git init` começa o controle de versão no seu projeto.

---

### 📌 Exemplo completo:

```bash
mkdir meu-projeto
cd meu-projeto
git init
```

Agora essa pasta já pode rastrear mudanças no código 🚀

Se quiser, posso explicar agora o `git add` 😉