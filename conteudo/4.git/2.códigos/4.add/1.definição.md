# `git add` é o comando que adiciona arquivos à área de stage (prepara para o commit).



















## ➕ `git add`

# `git add` é o comando que **adiciona arquivos à área de stage** (prepara para o commit).

Ou seja, ele diz ao Git:

> “Quero incluir esse arquivo no próximo commit.”

---

### 🔧 Adicionar um arquivo específico

```bash
git add index.html
```

---

### 📁 Adicionar todos os arquivos modificados

```bash
git add .
```

---

### 🧠 O que é “stage”?

É uma **área intermediária** entre:

- 📝 Arquivos modificados
    
- 📦 Commit final
    

Você escolhe exatamente o que vai entrar no commit.

---

### 🧠 Em resumo:

> `git add` prepara arquivos para serem salvos definitivamente com `git commit`.

Se quiser, posso explicar agora o `git commit` 🚀