## 📊 `git status`

# `git status` é o comando que **mostra o estado atual do repositório**.

Ele informa:

- 📁 Arquivos modificados
    
- ➕ Arquivos adicionados ao stage
    
- ❌ Arquivos não rastreados (untracked)
    
- ✔️ Se há commits pendentes
    

---

### 🔧 Exemplo:

```bash
git status
```

---

### 🧠 O que ele responde na prática?

Ele basicamente diz:

> “O que mudou desde o último commit?”

---

### 📌 Situações comuns:

- `modified` → arquivo foi alterado
    
- `untracked` → arquivo novo ainda não adicionado
    
- `staged` → pronto para commit
    

---

### 🧠 Em resumo:

> `git status` serve para você saber exatamente em que estado seu projeto está antes de fazer um commit.

Se quiser, posso explicar agora o `git add` 🔥