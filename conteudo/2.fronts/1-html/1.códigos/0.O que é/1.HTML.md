 # **HTML (HyperText Markup Language)** é a **linguagem de marcação usada para estruturar o conteúdo de páginas na web**.






















---

## 🧱 O que é HTML?

**HTML** é o **esqueleto de um site** 🦴

Ele diz:

- o que é **texto**
    
- o que é **imagem**
    
- o que é **botão**
    
- o que é **título**
    
- o que é **campo de formulário**
    

Sem HTML 👉 **o site nem existe** ❌🌐

---

## 🧠 Para que o HTML serve?

O HTML serve para **organizar o conteúdo** da página, como:

- 📰 Títulos
    
- ✍️ Parágrafos
    
- 🖼️ Imagens
    
- 🔗 Links
    
- 🔘 Botões
    
- 🧾 Formulários
    
- 📦 Caixas (divs, sections, etc.)
    

Ele **não cuida da beleza** ❌🎨  
👉 Quem cuida disso é o **CSS**

---

## 🧩 HTML funciona com TAGS

O HTML usa **tags**, que parecem assim:

```html
<p>Olá mundo</p>
```

Vamos quebrar isso 👇

- `<p>` → começa um parágrafo
    
- `Olá mundo` → o conteúdo
    
- `</p>` → fecha o parágrafo
    

📦 É como colocar algo **dentro de uma caixa**

---

## 🏗️ Estrutura básica de um HTML

Todo HTML tem essa base 👇

```html
<!DOCTYPE html>
<html>
  <head>
    <title>Meu site</title>
  </head>
  <body>
    <h1>Olá!</h1>
    <p>Esse é meu site</p>
  </body>
</html>
```

### Explicando como criança 👶

- `<!DOCTYPE html>` → diz: “ei navegador, isso é HTML!”
    
- `<html>` → envolve todo o site 🌐
    
- `<head>` → informações do site (não aparece na tela 🧠)
    
- `<body>` → tudo que **aparece** na tela 👀
    

---

## 🏷️ Exemplos de tags importantes

### 📰 Título

```html
<h1>Título grande</h1>
```

### ✍️ Texto

```html
<p>Um parágrafo</p>
```

### 🖼️ Imagem

```html
<img src="foto.png">
```

### 🔗 Link

```html
<a href="https://google.com">Ir ao Google</a>
```

### 📦 Caixa genérica

```html
<div>Conteúdo</div>
```

---

## 🧬 HTML usa ATRIBUTOS

Atributos dão **informações extras** às tags 🧠

```html
<img src="foto.png" alt="Minha foto">
```

- `src` → onde está a imagem
    
- `alt` → texto alternativo
    

---

## 🤝 HTML + CSS + JS

Eles trabalham juntos 👇

- 🧱 **HTML** → estrutura
    
- 🎨 **CSS** → aparência
    
- 🧠 **JavaScript** → comportamento
    

Exemplo:

- HTML cria o botão 🔘
    
- CSS deixa ele bonito 🎨
    
- JS faz ele funcionar ⚙️
    

---

## 🧠 Resumo ultra simples

- HTML = estrutura do site 🧱
    
- Usa tags 📦
    
- Diz **o que é cada coisa**
    
- Não cuida do visual ❌🎨
    

---

## 📘 Definição técnica (simples)

> **HTML (HyperText Markup Language)** é uma linguagem de marcação usada para estruturar o conteúdo de páginas web 🧱🌐✨

Se quiser, no próximo passo posso explicar:

- 📌 o que é tag semântica
    
- 📌 diferença entre div e section
    
- 📌 o que é head vs body
    
- 📌 como HTML se conecta com CSS
    

É só falar 😄