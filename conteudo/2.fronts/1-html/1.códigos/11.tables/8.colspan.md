# **`colspan`** é um **atributo** usado em `<td>` ou `<th>` para **mesclar células horizontalmente em uma tabela**.

Exemplo:

```html
<table>
  <tr>
    <th colspan="2">Informações do Produto</th>
  </tr>
  <tr>
    <td>Caneta</td>
    <td>R$ 2,50</td>
  </tr>
</table>
```