# **`rowspan`** é um **atributo** usado em `<td>` ou `<th>` para **mesclar células verticalmente em uma tabela**.

Exemplo:

```html
<table>
  <tr>
    <th rowspan="2">Produto</th>
    <th>Preço</th>
  </tr>
  <tr>
    <td>R$ 2,50</td>
  </tr>
</table>
```
