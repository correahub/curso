# **`dt`** é um elemento usado para **criar um termo dentro de uma lista de definição**.

Exemplo:

```html
<dl>
  <dt>HTML</dt>
  <dd>Linguagem de marcação</dd>
</dl>
```
<dl>
  <dt>HTML</dt>
  
</dl>
