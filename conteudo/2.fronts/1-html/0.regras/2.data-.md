# `data- é um atributo personalizado do HTML usado para armazenar informações extras dentro de uma tag, principalmente para uso com JavaScript.























## `data-*`

**Definição:**  
`data-*` é um **atributo personalizado do HTML** usado para armazenar **informações extras** dentro de uma tag, principalmente para uso com JavaScript.

Ele sempre começa com `data-` seguido de qualquer nome que você quiser.

---

## ✅ Exemplo simples

```html
<div data-personagem="Lortriel">
  Mago aprendiz
</div>
```

Aqui estamos guardando a informação `"Lortriel"` dentro do elemento.

---

## 🎯 Como acessar no JavaScript

```html
<script>
  const div = document.querySelector("div");
  console.log(div.dataset.personagem);
</script>
```

🔹 `dataset` permite acessar os atributos `data-*`.

---

## 🧠 Regra importante

Se você escrever:

```html
<div data-nome-completo="Tyler RavenWood"></div>
```

No JavaScript você acessa assim:

```js
element.dataset.nomeCompleto
```

O hífen vira **camelCase** automaticamente.

---

## 💡 Resumo direto (estilo que você gosta)

`data-*` → atributo HTML para guardar dados personalizados no elemento, geralmente usados pelo JavaScript.

---

Se quiser, posso te explicar a diferença entre `data-*` e atributos normais como `id`, `class`, `name` 👑