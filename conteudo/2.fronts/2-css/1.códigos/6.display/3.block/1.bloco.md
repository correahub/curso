# **`block`** é um **valor** do atributo `display` que define que **o elemento será exibido como um bloco**, ocupando toda a largura disponível, mesmo que seu conteúdo seja pequeno, e iniciando em uma nova linha não aceitando ficar na mesma linha que outros elementos, e ele aceita valores de altura e largura.

























🧱 Imagine que você está brincando com blocos de montar

Sabe aqueles **blocos de brinquedo** que você empilha um em cima do outro?  
Cada bloco ocupa **uma linha inteira**.  
Não dá pra colocar outro bloco ao lado, só **embaixo**.

Esses blocos são como os **elementos de nível bloco** no HTML.

---

## 📦 O que é um elemento de nível bloco?

É um **tipo de caixinha** (ou "elemento") que ocupa **toda a largura da tela** —  
mesmo que o conteúdo dentro dele seja pequeno!

---

### 🧠 Exemplo:

```html
<div>Eu sou um bloco!</div>
<p>Eu também sou!</p>
<h1>Eu também!</h1>
```

Todos esses — `<div>`, `<p>`, `<h1>`, `<section>`, `<article>`... —  
são **elementos de nível bloco**.

Eles sempre começam **em uma nova linha**, e empurram os outros pra **baixo**.

---

### 🖼️ Imagina assim:

```html
<div>🍎</div>
<p>🍌</p>
<h1>🍇</h1>
```

Vai aparecer assim:

```
🍎
🍌
🍇
```

Eles nunca ficam assim:

```
🍎 🍌 🍇
```

Porque **elemento de bloco não gosta de dividir a linha!** 😄

---

## 🧩 Resumindo com uma frase simples:

> "Elemento de nível bloco é uma caixinha que ocupa a linha toda.  
> Nada pode ficar ao lado dela. Só embaixo!"