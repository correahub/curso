# `order` é o **atributo** que define **a ordem de exibição de um item flexível**, permitindo reorganizar os elementos sem alterar o HTML, usando números, sendo o menor numero fica na frente do maior numero.




















---

# 🧸 O que é **order** no Flexbox?

Imagine que você tem vários brinquedos na mesa: um carrinho, uma boneca e um robô.  
Normalmente, você coloca eles na ordem que pegou: primeiro o carrinho, depois a boneca, depois o robô.

👉 **A propriedade `order` é como se você pudesse dizer: “Ei, boneca! Vá para o primeiro lugar!”, mesmo que ela tenha sido colocada depois.**  
Ou seja, você **muda a fila** dos elementos, sem precisar mexer no HTML.

---

# 🎯 Explicando de forma bem simples:

### 🧠 Como funciona?

Cada item do flex ganha um **número** (o `order`).

- Quem tem o **menor número** fica **primeiro**.
    
- Quem tem o **maior número** fica **por último**.
    
- O valor padrão é **0**.
    

É como se cada item tivesse um “número da fila”.  
Quem tiver o número mais baixo entra primeiro na fila! 🧑‍🏫➡️➡️➡️

---

# 🎨 Exemplo bem simples:

```html
<style>
  .container {
    display: flex;
    gap: 10px;
  }

  .a { order: 2; background: lightcoral; }
  .b { order: 0; background: lightgreen; }
  .c { order: 1; background: lightblue; }
</style>

<div class="container">
  <div class="a">A</div>
  <div class="b">B</div>
  <div class="c">C</div>
</div>
```

Mesmo que no HTML esteja A, B, C…  
👉 Na tela aparece: **B, C, A**  
Porque:

- B tem order **0** (menor → primeiro)
    
- C tem order **1**
    
- A tem order **2** (maior → último)
    


---

Se quiser, posso te mostrar:

- um exemplo com animação
    
- um onde o order muda com hover
    
- um exemplo comparando com `flex-direction`
    
- ou até uma metáfora ainda mais infantil 👶💛
    

Só pedir!