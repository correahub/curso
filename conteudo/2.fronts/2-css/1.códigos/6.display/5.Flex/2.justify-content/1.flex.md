# **`justify-content`** é o **atributo** que define **como os itens de um contêiner flexível são distribuídos ao longo do eixo primário**, podendo ser alinhados à esquerda, direita, centro, com espaço entre ou ao redor ↔️📏


















# 1. flex-start;`

👉 "Alinha os elementos no inicio do eixo principal"

```html
<div style="display: flex; justify-content: flex-start;">
  <div>🟥</div><div>🟩</div><div>🟦</div>
</div>
```

📦 Visual:

```
🟥🟩🟦       (os blocos colados na esquerda)
```

---

# 2. center;`

👉 "Centraliza os elementos no centro do eixo principal"

```html
<div style="display: flex; justify-content: center;">
  <div>🟥</div><div>🟩</div><div>🟦</div>
</div>
```

📦 Visual:

```
    🟥🟩🟦    (os blocos estão no centro)
```


### 3. `flex-end;`

👉 "Alinha os elementos no fim do eixo principal"

```html
<div style="display: flex; justify-content: flex-end;">
  <div>🟥</div><div>🟩</div><div>🟦</div>
</div>
```

📦 Visual:

```
       🟥🟩🟦 (os blocos colados na direita)
```


---

# 4. space-between;`

👉 "Alinha os elementos igualmente sem deixar espaços nas laterais."

```html
<div style="display: flex; justify-content: space-between;">
  <div>🟥</div><div>🟩</div><div>🟦</div>
</div>
```

📦 Visual:

```
🟥     🟩     🟦
```

Como se estivessem brincando de **esconde-esconde**! Cada um num canto. 😄

---

# 5. space-around;

👉 "Alinha os elementos igualmente ao redor das tags, deixando um pouco de espaços nas laterais"

```html
<div style="display: flex; justify-content: space-around;">
  <div>🟥</div><div>🟩</div><div>🟦</div>
</div>
```

📦 Visual (espaços iguais ao redor):

```
  🟥   🟩   🟦
```

Tem espaço dos dois ladinhos de cada um!

---

### 6. `justify-content: space-evenly;`

👉 "Alinha igualmente os elementos ao longo do eixo principal"

```html
<div style="display: flex; justify-content: space-evenly;">
  <div>🟥</div><div>🟩</div><div>🟦</div>
</div>
```

📦 Visual:

```
  🟥   🟩   🟦
```

Parece o `space-around`, mas aqui até o espaço **nas pontas** é igual! 🧮

---

## 🧁 Resumindo com carinho:

|`justify-content`|Onde ficam os blocos?|
|---|---|
|`flex-start`|Encostados no começo (esquerda ou topo)|
|`flex-end`|Encostados no fim (direita ou embaixo)|
|`center`|Bem no meio! ❤️|
|`space-between`|Espalhados com espaço só entre eles|
|`space-around`|Espaço ao redor de cada um|
|`space-evenly`|Espaços todos IGUAIS, até nas pontas da caixa|

---
