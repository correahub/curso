# **`gap`** é o **atributo** que define **o espaço entre itens de um layout flex ou grid**, controlando a distância entre eles sem usar margin.























---

# 🧸 O que é **gap**?

Imagine que você está organizando vários brinquedos no chão: carrinhos, bonecos, blocos de montar.  
Se você colocar um _do lado do outro_ sem espaço nenhum, parece tudo apertado 😵‍💫.

👉 **O `gap` é como deixar um espacinho entre cada brinquedo para ficar tudo mais organizado e bonito.**  
É simplesmente o **espaço entre os itens** dentro de um container flex.

---

# 🎯 Como entender isso de forma bem simples?

No Flexbox:

- Você tem vários itens lado a lado (ou um embaixo do outro).
    
- Às vezes você quer que eles tenham um espaçozinho entre si.
    
- Ao invés de colocar `margin` em cada um, você usa **gap** e pronto! ✨
    

### Gap é como dizer:

> “Entre cada item, deixe X pixels de espaço.”

---

# 🎨 Exemplo bem simples:

```html
<style>
  .container {
    display: flex;
    gap: 20px; /* espaço entre os itens */
  }

  div.item {
    width: 60px;
    height: 60px;
    background: skyblue;
  }
</style>

<div class="container">
  <div class="item"></div>
  <div class="item"></div>
  <div class="item"></div>
</div>
```

Resultado:  
Os quadrados ficam lado a lado com **20px de espaço entre eles**, mas sem espaço nas bordas do container — só entre os itens.

---

# 🔍 Gap também funciona no eixo vertical

Se o Flexbox estiver em **coluna**, o gap cria espaço vertical:

```css
.container {
  display: flex;
  flex-direction: column;
  gap: 10px;
}
```

---

# ✨ E também existe:

- **row-gap** → espaço entre as linhas
    
- **column-gap** → espaço entre as colunas  
    Mas no Flexbox o `gap` já resolve tudo.
    

---

# 📘 **Definição técnica simples**

**Gap adiciona um espaçamento externo entre os itens flex, na direção do eixo principal, sem usar margin.**

---

Se quiser, te mostro:

- a diferença entre `gap` e `margin`
    
- um exemplo com `wrap`
    
- um exemplo animado
    
- ou uma analogia mais infantilzinha 👶💛
    

Só pedir!