# `flex-flow` é o **atributo** que define **de forma abreviada a direção e a quebra de linha de um contêiner flexível**, combinando `flex-direction` e `flex-wrap`.





















---

# 🎀 O que é **flex-flow**?

Pense que o **flex-flow** é como um _comando duplo_ que junta **duas propriedades** do Flexbox:

- **flex-direction** ➝ decide **a direção** que os itens vão ficar (linha, coluna, etc) ➡️⬇️
    
- **flex-wrap** ➝ decide se eles **quebram linha** ou ficam tudo espremido na mesma fila 🧻📦
    

Então o **flex-flow** é só um **atalho** para usar **os dois ao mesmo tempo** ✨

---

# 🎨 Como funciona na prática?

Você pode escrever assim:

```css
container {
  flex-flow: row wrap;
}
```

Isso significa:

- **row** ➝ itens em uma linha (horizontal)
    
- **wrap** ➝ podem descer para outra linha se não couberem
    

É exatamente o mesmo que escrever:

```css
container {
  flex-direction: row;
  flex-wrap: wrap;
}
```

---

# 📦 Exemplo visual (imaginando como uma criança 🎈)

Imagine que você tem vários brinquedos numa mesa.

- **flex-direction** escolhe **se vai colocar todos em linha** (um do lado do outro) ou **em coluna** (um em cima do outro).
    
- **flex-wrap** decide se **quando a mesa ficar cheia**, os brinquedos podem **ir para uma segunda linha**.
    

Então **flex-flow** é como dizer:

> “Quero os brinquedos em **linha**, e se não couberem, podem **descer para outra linha**.” 🎁➡️📦  
> Ou  
> “Quero os brinquedos em **coluna**, mas **sem quebrar**, tudo em uma coluna só.” 🎁⬇️

---

# 🧩 Principais combinações

|Valor|Explicação|
|---|---|
|`row nowrap`|Linha sem quebra. (Padrão)|
|`row wrap`|Linha com quebra.|
|`column nowrap`|Coluna sem quebra.|
|`column wrap`|Coluna com quebra (quebra para o lado).|

---

# 🚀 Quer uma definição curtinha?

**flex-flow:** combina a direção e a quebra de linha dos itens flex. 🎯📦

---
