# **pseudo-classe** é um **tipo de seletor** que define **um estado especial de um elemento**, como quando o mouse passa por cima, quando ele é clicado ou quando está ativo.

















---

# ⭐ O que é uma **pseudo-class** no CSS?

Pense que cada elemento (uma `div`, um `button`, um `a`, etc.) é como um **bonequinho** no seu site 👤.

A **pseudo-class** é como uma **emoção ou estado especial** desse bonequinho que só aparece em certas situações.

Por exemplo:

- Quando você **passa o mouse** em cima de um botão → ele fica “feliz” 😄
    
- Quando você **clica** → ele fica “surpreso” 😲
    
- Quando um link já foi **visitado** → ele fica “com memória” 🧠
    
- Quando um campo do formulário está **focado** → ele fica “atento” 👀
    

A pseudo-class permite que o CSS **mude a aparência** do elemento **nesses estados especiais**, sem precisar criar classes novas.

---

# 🎨 Exemplo simples

```css
button:hover {
  background-color: red;
}
```

**Explicação:**  
`hover` é uma pseudo-class que significa “quando o mouse estiver em cima”.  
Então o botão só fica vermelho **no momento em que o mouse passa sobre ele**.

---

# 🧠 Definição simples e técnica (como você pediu)

**Pseudo-class é uma palavra especial no CSS que representa um _estado temporário_ ou _condição específica_ de um elemento, permitindo aplicar estilos somente quando esse estado acontece.** 🎯✨

---

# 💡 Exemplos de pseudo-classes muito usadas

|Pseudo-class|Quando acontece|
|---|---|
|`:hover`|quando o mouse está em cima|
|`:active`|quando está sendo clicado|
|`:focus`|quando o elemento recebeu foco|
|`:visited`|quando o link já foi visitado|
|`:first-child`|quando é o primeiro filho|
|`:last-child`|quando é o último filho|
|`:checked`|quando checkbox ou radio está marcado|

---
