# **`:read-only`** é uma **pseudo-classe** que define **o estado de elementos de formulário que são somente leitura**, permitindo visualizar o valor mas não alterá-lo.
























---

# 📘 O que é **`:read-only`** no CSS?

**`:read-only` é uma pseudo-class que seleciona elementos que só podem ser lidos, mas não podem ser editados.**

Geralmente usada em:

- `<input>`
    
- `<textarea>`
    

Quando eles têm o atributo **`readonly`**.

---

## ✔️ Explicação simples (igual às suas definições):

**Pseudo-class `:read-only` pega elementos que podem ser lidos, mas não podem ser alterados.** 📘✨

---

## 🧒 Explicando como para uma criança:

Imagine que você tem um caderno aberto na sua frente.  
Você pode **ler** tudo que está escrito nele,  
mas **não pode escrever** nada porque a página está “protegida”.

O CSS olha para esse campo e fala:

👀 “Você só pode ser lido? Então vou te estilizar como read-only!”

É como um campo que diz:

📖 “Pode olhar, mas não pode mexer!”

---

## 🧪 Exemplo simples:

### 🔗 HTML:

```html
<input type="text" value="Texto editável">

<input type="text" value="Somente leitura" readonly>

<textarea readonly>Você pode ler este texto, mas não pode editar.</textarea>
```

### 🎨 CSS:

```css
input:read-only,
textarea:read-only {
  background: #f5f5f5;
  border: 2px dashed gray;
  color: #555;
}
```

👉 Apenas os campos com **readonly** recebem o estilo.

---

## 🎯 Definição técnica simples:

**`:read-only` é uma pseudo-class que seleciona elementos que estão em modo somente leitura (`readonly`) e não podem ser editados.** 📖✨

---
