# **`:has()`** é uma **pseudo-classe** que define **um elemento pai com base na presença ou estado de seus elementos filhos**, permitindo aplicar estilos dependendo do que existe dentro dele.

















---

# 🔍 O que é **`:has()`** no CSS?

 :has() é uma pseudo-class que aplica estilos aos elementos com base em seus filhos.

Ela olha _para dentro_ do elemento e diz:

👉 “Se dentro de você existir tal coisa, eu aplico o estilo.”

É como fazer um **selecionador de pai**, algo que antes não existia no CSS.

---

## 📘 Explicação simples (no estilo das suas frases):

**Pseudo-class `:has()` é um seletor que aplica estilo a um elemento que contém um outro seletor específico dentro dele.** 🔍✨

---

# 🧒 Explicando como para uma criança:

Imagina que você tem uma caixa grande (um div).  
Dentro dela podem existir brinquedos (outros elementos).

O CSS usa o `:has()` como se fosse:

👀 “Deixa eu olhar _dentro_ dessa caixa.  
Se eu encontrar um carro vermelho lá dentro, então eu vou pintar a caixa inteira de azul.”

Então o `:has()` escolhe **o elemento de fora**, baseado **no que está dentro** dele.

É como dizer:

🎯 “Se esse elemento tiver X dentro, então mude ele mesmo.”

---

# 🧪 Exemplos simples

## ⭐ Exemplo 1 — Selecionar um `<div>` que contém um `<p>`:

```css
div:has(p) {
  border: 2px solid red;
}
```

👉 Qualquer `<div>` que tiver um `<p>` dentro ganha uma borda vermelha.

---

## ⭐ Exemplo 2 — Selecionar um `<label>` quando o `<input>` dentro dele estiver `:checked`:

```css
label:has(input:checked) {
  color: green;
  font-weight: bold;
}
```

👉 O `label` muda quando o input for marcado ✔️  
Isso é poderosíssimo!

---

## ⭐ Exemplo 3 — Estilizar um card que contém um botão desabilitado:

```css
.card:has(button:disabled) {
  opacity: 0.5;
}
```

👉 Se dentro do `.card` tiver um botão desativado, o card inteiro fica mais apagado.

---

# 🎯 Definição técnica simples:

**`:has()` é uma pseudo-class relacional que seleciona um elemento baseado em seus descendentes ou conteúdo interno.** 🔍✨

---

Se quiser, posso te ensinar agora usos avançados do `:has()` — como validar formulários sem JS, criar menus, mudar estilo do pai quando o filho recebe foco, etc! 😄🔥