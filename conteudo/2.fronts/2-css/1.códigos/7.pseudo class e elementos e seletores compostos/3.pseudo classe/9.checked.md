# `:checked` é uma **pseudo-classe** que define **o estado de elementos de formulário que estão marcados**, como `checkbox` e `radio`.




















---

# ☑️ O que é **`:checked`** no CSS?

**`:checked` é uma pseudo-class que ativa um estilo quando um elemento está marcado.**  
Ela funciona com:

- ✔️ _checkbox_ (`<input type="checkbox">`)
    
- 🔘 _radio button_ (`<input type="radio">`)
    
- 📤 _option_ dentro de `<select>`
    

---

## 🧒 Explicando como para uma criança:

Imagine que você tem várias caixinhas para marcar.  
Quando você dá um clique e coloca um ✔️ dentro da caixinha, o CSS percebe e fala:

🎯 “Opa! Agora você está marcado! Vou mudar sua aparência!”

Esse momento — quando a caixinha está **marcada** — é quando o `:checked` funciona.

É como se o CSS estivesse o tempo todo observando e dizendo:

👀 “Marcado? Então muda o estilo.  
Não marcado? Então fica normal.”

---

## 🧪 Exemplo simples:

### 🔗 HTML:

```html
<input type="checkbox" id="switch">
<label for="switch">Clique aqui</label>
```

### 🎨 CSS:

```css
#switch:checked + label {
  color: red;
  font-weight: bold;
}
```

👉 Quando você marcar a caixinha, o texto do label fica vermelho e em negrito.

---

## 🎯 Definição técnica simples (como você pediu):

**`:checked` é uma pseudo-class que seleciona elementos marcados, como checkboxes, radio buttons e opções selecionadas.** ☑️📘✨

---

Se quiser, posso te mostrar como criar um menu completo só com `:checked` (sem JavaScript)! 😊🔥