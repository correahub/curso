# :last-child é uma pseudo-classe que define **o último elemento filho dentro de um elemento pai**, aplicando estilos apenas a ele.


















O que é `:last-child`?

Agora imagine a mesma fila de crianças, mas você olha para o **último** da fila:

👦👧👧👧  
↑  
Esse é o **último**.

O `:last-child` estiliza **somente** o último filho do elemento.

---

## 🎨 Exemplo

```css
div p:last-child {
  color: blue;
}
```

**O que acontece?**  
Apenas o **último** `<p>` dentro da `<div>` fica azul.

---

