# `:hover` é uma **pseudo-classe** que define **o estado de um elemento quando o mouse passa por cima dele**, permitindo aplicar estilos ao interagir com o cursor.























---

# 🖱️ O que é `:hover`?

Imagine que cada elemento da página (um botão, um link, uma imagem, etc.) é como um **objeto na mesa** 🧱.

Quando você **aproxima a mão** desse objeto, sem tocar, ele reage.  
No computador, quem faz esse papel é o **mouse**.

Então, quando o mouse **passa por cima** de um elemento, ele entra no estado:

> _“Opa, o mouse está aqui em cima de mim!”_ 😄

Esse momento é o `:hover`.

---

# 🎨 Exemplo simples

```css
button:hover {
  background-color: red;
}
```

**O que acontece?**  
Quando o mouse passa por cima do botão, ele muda de cor para vermelho.

---

# 📌 O `:hover` funciona em:

- botões
    
- links
    
- divs
    
- imagens
    
- qualquer elemento visível na tela
    

---

# 🧠 Definição simples e técnica

**`:hover` é uma pseudo-class que aplica estilos quando o mouse está sobre o elemento.** 🎯✨🖱️