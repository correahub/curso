# **`:not()`** é uma **pseudo-classe** que define **elementos que não correspondem a um seletor específico**, permitindo excluir certos elementos da aplicação de estilos.

















---

# 🚫 O que é `:not()`?

Imagine que você está escolhendo brinquedos para brincar 🎲🧸🚗.  
Aí você diz:

> _“Eu quero todos… **menos** o carrinho vermelho!”_ 🚗❌

O `:not()` faz exatamente isso no CSS:  
Ele **seleciona todos os elementos**, **exceto** os que você colocar dentro dos parênteses.

---

# 🎨 Exemplo simples

```css
p:not(.especial) {
  color: red;
}
```

**O que acontece?**  
Todos os `<p>` ficam vermelhos, **menos** os que têm a classe `.especial`.

---

# 📌 Para que serve?

- excluir um tipo de elemento
    
- excluir uma classe específica
    
- excluir um estado
    
- aplicar um estilo “para todos, menos…”
    

É como criar uma **regra do contra**. 😄

---

# 🧠 Definição simples e técnica

**`:not()` é uma pseudo-class que seleciona todos os elementos que _não_ combinam com o seletor especificado.** 