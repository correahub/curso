# **`:disabled`** é uma **pseudo-classe** que define **o estado de elementos de formulário que estão desativados**, impedindo interação do usuário.


















---

# 🔘 O que é **`:disabled`** no CSS?

**`:disabled` é uma pseudo-class que seleciona elementos que estão desativados**, ou seja, aqueles que **não podem ser usados**, clicados ou preenchidos.

Funciona com elementos de formulário:

- `<input>`
    
- `<button>`
    
- `<select>`
    
- `<textarea>`
    

---

## 📘 Explicação simples (igual às suas definições):

**Pseudo-class `:disabled` é um seletor que pega elementos que estão desativados e não podem ser usados.** 🔘✨

---

## 🧒 Explicando como para uma criança:

Imagine que você tem um botão que está “dormindo”.  
Você tenta clicar, mas ele não responde.  
Ou uma caixinha de texto que não deixa você digitar nada.

O CSS olha para esses elementos “adormecidos” e fala:

😴 “Ah, você está desligado… então vou aplicar o estilo de desativado!”

Esse momento — quando o elemento não pode ser usado — é quando o `:disabled` funciona.

---

## 🧪 Exemplo simples:

### 🔗 HTML:

```html
<input type="text" placeholder="Caixa ativa">
<input type="text" placeholder="Caixa desabilitada" disabled>

<button>Botão ativo</button>
<button disabled>Botão desabilitado</button>
```

### 🎨 CSS:

```css
input:disabled,
button:disabled {
  background: #eee;
  color: gray;
  border: 2px solid #ccc;
  cursor: not-allowed;
}
```

👉 Apenas os elementos **desabilitados** ficam cinzas e não utilizáveis.

---

