# **`:valid`** é uma **pseudo-classe** que define **o estado de um elemento de formulário quando seu valor é considerado válido**, de acordo com as regras definidas (como `required`, `type` ou `pattern`)



















---

# ✅ O que é `:valid`?

Imagine que você tem um **campo de formulário** (como um input de email, número, texto com regras, etc.).  
Esse campo é como uma **caixinha que só aceita certas coisas** 📦✔️.

Quando você digita algo **correto**, algo que segue as regras do campo, o navegador diz:

> _“Sim! Isso está certinho! Está válido!”_ 😄✨

O `:valid` serve para estilizar o elemento **quando o valor dele está correto** de acordo com as regras do HTML.

---

# 🎨 Exemplo simples

```html
<input type="email" placeholder="Digite seu email">
```

```css
input:valid {
  border: 2px solid green;
}
```

**O que acontece?**  
Quando o usuário digita um email válido (como `teste@gmail.com`), o input fica verde.  
Se digitar algo errado (como `aaaaa`), ele **não** recebe esse estilo.

---

# 📌 Onde o `:valid` funciona?

Ele funciona quando o campo tem algum tipo de **validação automática**, como:

- `type="email"`
    
- `type="number"`
    
- `type="url"`
    
- `required`
    
- `min` / `max`
    
- `pattern="..."`
    

Se o valor **segue a regra**, o elemento entra no estado `:valid`.

---
