# **pseudo-elemento** é um **tipo de seletor** que define **uma parte específica de um elemento**, como a primeira letra, a primeira linha ou conteúdos gerados automaticamente.



















---

# 🌟 O que é um **pseudo-elemento**?

Imagine que você tem um livro.  
Agora imagine que você consegue **pegar um pedacinho desse livro** sem precisar rasgar ele:  
— Pode ser **a primeira letra**,  
— Pode ser **a primeira linha**,  
— Pode ser **uma caixinha invisível antes do texto**,  
— Ou **uma caixinha invisível depois do texto**.

Você não criou nada novo no HTML.  
Você só pediu para o CSS “imaginar” uma parte desse elemento e estilizar isso.

👉 **Pseudo-elemento é uma parte imaginária de um elemento que o CSS cria para você estilizar.**

É como acessar um **pedaço especial** que não existe sozinho no HTML.

---

# 🔍 Exemplos simples

## ⭐ `::before`

Coloca algo **antes** do elemento, como se criasse um "filho fantasma".

```css
p::before {
  content: "👉 ";
}
```

## ⭐ `::after`

Coloca algo **depois** do elemento.

```css
p::after {
  content: " ✔";
}
```

## ⭐ `::first-letter`

Estiliza **só a primeira letra**.

```css
p::first-letter {
  font-size: 40px;
}
```

## ⭐ `::first-line`

Estiliza **só a primeira linha** do texto.

---

# 🍬 Explicando como para uma criança

Pense assim:

💡 _“Um pseudo-elemento é como um pedacinho mágico de um elemento que o CSS te deixa pegar e pintar."_ ✨

Você não vê ele no HTML.  
Ele só existe enquanto o CSS estiver sendo aplicado.

