# **`nesting()`** é o **recurso** que permite **escrever identificadores dentro de outros identificadores**, organizando melhor os estilos e evitando repetição de identificadores. O & significa este identificador atual.





















```css
.button {
  background: blue;

  &:hover {
    background: red;
  }
}
```


















---

## 🌳 O que é _Nesting_ no CSS?

**Nesting** significa **escrever regras CSS dentro de outras regras**, seguindo a mesma ideia de _pai e filho_ do HTML 👨‍👧‍👦.

Em vez de repetir o seletor várias vezes, você **coloca um seletor dentro do outro**.

---

## ❌ CSS sem nesting (forma antiga)

```css
.card {
  background: white;
}

.card h2 {
  color: black;
}

.card p {
  color: gray;
}
```

Aqui você precisa repetir `.card` toda hora 😓

---

## ✅ CSS com nesting (forma moderna)

```css
.card {
  background: white;

  h2 {
    color: black;
  }

  p {
    color: gray;
  }
}
```

👉 Agora fica claro que:

- `h2` está **dentro** de `.card`
    
- `p` está **dentro** de `.card`
    

Mais limpo, mais organizado 🧼✨

---

## 🧠 Como pensar em Nesting?

Pense assim:

> 📦 `.card` é uma caixa  
> 🏷️ `h2` e `p` são coisas **dentro da caixa**

Tudo que está aninhado só vale **dentro daquele elemento pai**.

---

## 🎯 Usando `&` (muito importante!)

O símbolo `&` significa **“este seletor atual”** 👈

### Exemplo com pseudo-class:

```css
.button {
  background: blue;

  &:hover {
    background: red;
  }
}
```

Isso vira:

```css
.button:hover {
  background: red;
}
```

---

### Outro exemplo com classe filha:

```css
.menu {
  &.active {
    color: green;
  }
}
```

Resultado:

```css
.menu.active {
  color: green;
}
```

---

## 🔗 Nesting com seletores familiares

```css
ul {
  li {
    list-style: none;

    a {
      text-decoration: none;
    }
  }
}
```

Isso significa:

```css
ul li a { ... }
```

Tudo encadeado como uma árvore 🌳

---

## ⚠️ Cuidado com Nesting demais

❌ Evite isso:

```css
.a {
  .b {
    .c {
      .d {
        color: red;
      }
    }
  }
}
```

👉 Fica difícil de manter 😵  
👉 Pode causar problemas de especificidade

📌 Regra simples:

> **Nesting é bom, exagero é ruim**

---

## 🧩 Compatibilidade

✔️ CSS Nesting **já funciona nos navegadores modernos**  
❗ Antigamente só existia em pré-processadores como **Sass / SCSS**

Hoje é **CSS puro mesmo** 🎉

---

## 📌 Resumo rápido

- Nesting = CSS dentro de CSS 📦
    
- Segue a hierarquia do HTML 🌳
    
- Evita repetição ✂️
    
- Usa `&` para pseudo-classes e combinações 🔗
    

---

## 📘 Definição técnica (como você pediu 👇)

**CSS Nesting** é um recurso que permite escrever seletores CSS aninhados dentro de outros seletores, representando relações hierárquicas entre elementos e reduzindo repetição de código.