# **`fit-content`** é o **valor** que faz um elemento **ter o tamanho exato do seu conteúdo**, respeitando limites mínimos e máximos quando existirem.






























### 🧸 O que é `width: fit-content`?

É como dizer assim pra caixinha (a **div** por exemplo):

> "Queridinha, fique **do tamanho exato** do que tem dentro de você. Nem maior, nem menor. Só o necessário!"

---

### 🧃 Exemplo simples:

Imagina isso no HTML:

```html
<div class="caixinha">Oi!</div>
```

E no CSS:

```css
.caixinha {
  background-color: yellow;
  width: fit-content;
  padding: 10px;
}
```

🟡 Nesse exemplo:

- O fundo é amarelo.
    
- O texto dentro é "Oi!".
    
- E a caixinha **fica do tamanho certinho** da palavra "Oi!" (mais um espacinho por causa do `padding`).
    
- Ela **não ocupa a tela toda**, nem 80%, nem 100%. Só o **tamanho necessário pra caber o conteúdo**.
    

---

### 🧩 Comparando:

|Código CSS|O que acontece?|
|---|---|
|`width: 100%`|A div ocupa **toda a largura da tela**|
|`width: 80%`|Ocupa **80% da largura da tela**|
|`width: 200px`|Fica com **tamanho fixo de 200px**|
|`width: fit-content`|Fica com **o tamanho exato do conteúdo**!|
