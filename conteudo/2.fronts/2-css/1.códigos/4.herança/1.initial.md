# **`initial`** é o **valor** que faz um atributo **voltar para o seu valor padrão original**, como definido pelo navegador.























---

## 📌 O que é `initial`?

**`initial` significa:  
“Esquece tudo e volta para o valor padrão da propriedade.”** 🔄

👉 Ele **zera a influência do pai**, do navegador e de qualquer herança.

---

## 🧠 Ideia bem simples

Imagine um botão 🎮:

- Você mudou várias configurações
    
- Mas aperta **reset**
    
- Tudo volta para o **padrão de fábrica**
    

👉 Isso é `initial`.

---

## 🧠 Definição técnica (bem simples)

**`initial` redefine a propriedade para o valor padrão do CSS.**


---

## ✍️ Exemplo prático

```css
p {
  color: initial;
}
```

🔹 O que acontece?

- Mesmo que o pai seja vermelho
    
- O `p` **não herda**
    
- Ele volta para o valor padrão → geralmente `black`
    

---

## 📎 Ponto MUITO importante

👉 **`initial` NÃO herda nada do pai**, mesmo se a propriedade normalmente herdasse.

Exemplo:

```css
div {
  color: red;
}

p {
  color: initial;
}
```

✅ O `p` fica **preto**, não vermelho.

---

## 🧪 Exemplo com propriedade que não herda

```css
div {
  border: 3px solid blue;
}

p {
  border: initial;
}
```

🔹 Resultado:

- O `p` fica **sem borda**, porque o valor inicial de `border` é `none`
    

---

## 🧠 Comparação rápida (mental)

|Valor|O que faz|
|---|---|
|`inherit`|Copia do pai 👨‍👦|
|`initial`|Volta ao padrão 🔄|

---

## 🧩 Quando usar `initial`?

Use quando você quer:

- Remover herança
    
- Ignorar estilos globais
    
- Resetar um estilo específico
    

---

## 🧠 Frase para memorizar

👉 **`initial` = valor padrão do CSS, sem herança** 🔄📦

---

## 🧾 Definição curta (exata, como você pediu)

**`initial` é um valor que redefine a propriedade para o seu valor padrão definido pelo CSS, ignorando a herança.** 🔄🧾

✨ Quer que eu explique agora o **`unset`** (que mistura `inherit` e `initial`) ou o **`revert`**?