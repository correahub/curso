# **`unset`** é o **valor** que faz um atributo **herdar o valor do elemento pai se ele for herdável**, ou **voltar ao valor padrão se não for**.






















---

## 📌 O que é `unset`?

**`unset` significa:  
“Depende: se a propriedade herda, herda.  
Se não herda, volta ao padrão.”** 🤔🔄

👉 Ele **decide sozinho** entre `inherit` e `initial`.

---

## 🧠 Ideia bem simples

Imagine uma regra mágica 🪄:

- Se a propriedade **normalmente copia do pai** → copia
    
- Se **normalmente não copia do pai** → volta ao padrão
    

👉 Isso é `unset`.

---

## 🧠 Definição técnica (bem simples)

**`unset` faz a propriedade se comportar como `inherit` se ela for herdável, ou como `initial` se ela não for.** 

---

## ✍️ Exemplo com propriedade que herda

```css
div {
  color: red;
}

p {
  color: unset;
}
```

🔹 O que acontece?

- `color` **herda por padrão**
    
- `unset` → age como `inherit`
    
- O `p` fica **vermelho**
    

---

## ✍️ Exemplo com propriedade que NÃO herda

```css
div {
  border: 2px solid blue;
}

p {
  border: unset;
}
```

🔹 O que acontece?

- `border` **não herda**
    
- `unset` → age como `initial`
    
- O `p` fica **sem borda**
    

---

## 📎 Resumo mental rápido

|Tipo de propriedade|`unset` faz o quê|
|---|---|
|Herdável|Copia do pai (`inherit`) 👨‍👦|
|Não herdável|Volta ao padrão (`initial`) 🔄|

---

## 🧩 Quando usar `unset`?

Use quando você quer:

- Remover um estilo sem pensar se herda ou não
    
- Criar CSS mais genérico
    
- Resetar estilos com segurança
    

---

## 🧠 Frase para memorizar

👉 **`unset` = herda se puder, senão reseta** 🧬🔄

---

## 🧾 Definição curta (exata, como você gosta)

**`unset` é um valor que aplica herança para propriedades herdáveis e valor inicial para propriedades não herdáveis.** 