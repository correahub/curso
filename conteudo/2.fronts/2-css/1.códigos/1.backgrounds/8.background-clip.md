# background-clip é o atributo que define até qual área da caixa do elemento o background pode ser desenhado.



















---

### 🔹 Com o valor `border-box`O fundo se estende **até a borda** do elemento (valor padrão).

```css
background-clip: border-box;
```

---















### 🔹Com o valor `padding-box` O fundo vai **até o padding**, sem cobrir a borda.

```css
background-clip: padding-box;
```

---













### 🔹Com o valor `content-box`O fundo aparece **apenas no conteúdo**, sem padding nem borda.

```css
background-clip: content-box;
```

---













### 🔹 Com o valor `text` _(uso comum)_ O fundo é **recortado pelo texto**, ficando visível só nas letras.

```css
background-clip: text;
color: transparent;
```

---

