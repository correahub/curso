# **`background-attachment`** é o atributo que define **se a imagem de fundo fica fixa ou se rola junto com a página ou com o elemento**.


















### 🔹 Com o valor `scroll` **O fundo se move junto com a rolagem da página** (valor padrão).

```css
body {
  background-attachment: scroll;
}
```

---













### 🔹 Com o valor `fixed` **O fundo fica fixo na tela**, enquanto o conteúdo rola por cima.

```css
body {
  background-attachment: fixed;
}
```

---

