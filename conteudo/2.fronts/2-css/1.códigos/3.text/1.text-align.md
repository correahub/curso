# **`text-align`** é o **atributo** que define **o alinhamento horizontal dos textos dos elementos.



















---

### 🔹 Com o valor `left`Alinha o texto à **esquerda**.

```css
p {
  text-align: left;
}
```

---










### Com o valor `right` Alinha o texto à **direita**.

```css
p {
  text-align: right;
}
```

---












### Com o valor  `center` Centraliza o texto horizontalmente.

```css
p {
  text-align: center;
}
```

---














### Com o valor  `justify` Justifica o texto, alinhando **tanto à esquerda quanto à direita**, ajustando os espaços entre as palavras.

```css
p {
  text-align: justify;
}
```

---
