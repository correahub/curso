# **`text-indent`** é o **atributo** que define **o recuo da primeira linha do texto** dentro de um paragrafo.























## 📘 O que é `text-indent`?

A propriedade `text-indent` do CSS **define o recuo da primeira linha de um parágrafo**.

Ou seja, ela serve para empurrar a **primeira linha de um bloco de texto** para a direita (ou esquerda, se for um valor negativo).

---

### 🧠 Por que usar `text-indent`?

Ela é muito usada para:

- Criar visual de **parágrafo bem formatado**, como em livros e textos acadêmicos.
    
- Melhorar a **leitura e organização do conteúdo textual**.
    

---

## 🔧 Sintaxe

```css
selector {
  text-indent: valor;
}
```

- `valor`: pode ser em `px`, `em`, `%`, etc.
    
    - `px` → pixels
        
    - `em` → relativo ao tamanho da fonte
        
    - `%` → relativo à largura do **bloco** (ex: `width` da `div`)
        

---

## 🧪 Exemplo Prático

### HTML

```html
<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Exemplo de text-indent</title>
  <style>
    p {
      text-indent: 40px;
      width: 300px;
      font-size: 16px;
      line-height: 1.5;
      text-align: justify;
    }
  </style>
</head>
<body>

  <p>
    Este é um parágrafo de exemplo. Veja como a primeira linha está mais para a direita. Esse recuo foi feito usando a propriedade <code>text-indent</code>.
  </p>

</body>
</html>
```

### 💡 O que acontece nesse exemplo:

- A primeira linha do parágrafo é **recuada 40 pixels para a direita**.
    
- As outras linhas continuam normalmente, alinhadas à esquerda.
    
- É como começar a escrever um parágrafo com um **espaço no início**, como se fosse uma margem só na primeira linha.
    

---

## 🔄 Outros exemplos de uso

```css
p {
  text-indent: 2em; /* recua duas vezes o tamanho da fonte */
}

p {
  text-indent: -20px; /* recua a primeira linha para a esquerda */
}

p {
  text-indent: 10%; /* recua 10% da largura do container */
}
```

---

## ⚠️ Dica importante para iniciantes

- `text-indent` **só funciona em blocos de texto**, como `<p>`, `<div>`, etc.
    
- Ele **não afeta** elementos inline como `<span>`.
    

---

Se quiser, posso te ajudar a experimentar isso no CodePen ou criar um mini-projeto com diferentes estilos de parágrafos. Quer?