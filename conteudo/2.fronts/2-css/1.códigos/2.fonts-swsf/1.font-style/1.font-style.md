# **`font-style`** é o **atributo** que define **o estilo da fonte**, como normal, itálico ou oblíquo ✍️📄





























## 🔤 O que é `font-style`?

A propriedade `font-style` em CSS serve para **alterar o estilo da fonte**, especialmente se ela será **normal**, **itálica** ou **obliqua (inclinada)**.

Ela é usada principalmente para **dar ênfase em textos**, como títulos ou palavras importantes.

---

## 🧱 Estrutura básica:

```css
seletor {
  font-style: valor;
}
```

---

## 📘 Os valores possíveis de `font-style`

### 1. `normal`

- **Significado**: A fonte será exibida normalmente, **sem itálico ou inclinação**.
    
- **Quando usar**: Quando você quiser remover o itálico de um texto.
    

```css
p {
  font-style: normal;
}
```

---

### 2. `italic`

- **Significado**: O texto aparece em **itálico**, ou seja, com letras inclinadas para a direita.
    
- **Quando usar**: Para **enfatizar** palavras, como nomes de livros, frases importantes, etc.
    

```css
em {
  font-style: italic;
}
```

> O elemento `<em>` no HTML já vem com `font-style: italic` por padrão.

---

### 3. `oblique`

- **Significado**: O texto também aparece **inclinado**, mas é **gerado artificialmente** pelo navegador se a fonte não tiver uma versão itálica.
    
- **Diferença entre `italic` e `oblique`**:
    
    - `italic` usa uma **versão verdadeira da fonte em itálico**.
        
    - `oblique` **inclina** a fonte normal, como se fosse "esticada".
        

```css
div.inclinado {
  font-style: oblique;
}
```

> Dica: Use `italic` sempre que puder. Só use `oblique` se quiser um efeito inclinado onde não há itálico real.

---

## 🧪 Exemplo completo:

```html
<!DOCTYPE html>
<html>
<head>
  <style>
    p.normal {
      font-style: normal;
    }

    p.italico {
      font-style: italic;
    }

    p.inclinado {
      font-style: oblique;
    }
  </style>
</head>
<body>

  <p class="normal">Texto com estilo normal</p>
  <p class="italico">Texto com estilo itálico</p>
  <p class="inclinado">Texto com estilo oblíquo</p>

</body>
</html>
```

---

## 🧠 Dicas para iniciantes:

- 📌 `font-style` **não muda a fonte em si** (para isso usamos `font-family`).
    
- 📌 Ele só muda o **estilo (inclinação)** da fonte atual.
    
- 📌 Você pode combinar com outras propriedades como `font-weight` (negrito), `font-size`, etc.
    

---

## 💬 Resumo:

|Valor|Efeito|
|---|---|
|`normal`|Texto sem itálico|
|`italic`|Texto em itálico real|
|`oblique`|Texto inclinado artificial|

---

Se quiser, posso te dar alguns desafios com `font-style` para praticar. Deseja isso?