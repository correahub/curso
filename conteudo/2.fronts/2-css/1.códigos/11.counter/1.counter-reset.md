# counter-reset é o atributo que define o contador e decide de qual valor o contador ira iniciar.




















---

## 🔄 O que é `counter-reset`?

Pense no `counter-reset` como um **botão de “zerar o contador”** 🔘

Ele serve para:  
👉 **criar um contador**  
👉 **definir o número inicial dele**

Sem `counter-reset`, **o contador nem existe** ❌

---

## 🧠 O que ele faz na prática?

Quando o CSS encontra:

```css
counter-reset: lista;
```

Ele entende:

> “Crie um contador chamado `lista` e comece do zero.”

📌 **Importante:**

- Ele **não mostra números**
    
- Ele **não aumenta números**
    
- Ele **apenas cria e reinicia**
    

---

## 🔢 Começar em outro número

Você pode dizer em qual número o contador começa:

```css
counter-reset: lista 5;
```

🧮 Resultado:

- Primeiro número será **6** (porque o incremento soma depois)
    

---

## 📦 Onde usar o `counter-reset`?

### ✔ Usar no elemento pai

Normalmente você usa no **pai** dos elementos que serão numerados:

```css
ul {
  counter-reset: item;
}
```

🧠 Assim, cada `<ul>` começa a contagem do zero.

---

### ✔ Reiniciar várias vezes

Se você colocar `counter-reset` em vários lugares, o contador **reinicia em cada um deles** 🔄

```css
section {
  counter-reset: passo;
}
```

Cada `<section>` começa do passo 1.

---

## 🔗 Criar vários contadores ao mesmo tempo

```css
counter-reset: capitulo 0 subcapitulo 0;
```

📚 Você pode ter vários contadores diferentes.

---

## ❌ O que `counter-reset` NÃO faz

❌ Não exibe números  
❌ Não incrementa  
❌ Não funciona sozinho

Ele só **prepara o contador** 🧰

---

## 🧠 Resumo mental (bem curtinho)

> **counter-reset** = cria o contador e define onde ele começa 🔄

---

## 📘 Definição técnica (simples)

> **`counter-reset`** é uma propriedade CSS usada para **criar ou reiniciar um contador**, definindo seu valor inicial antes de ser incrementado por `counter-increment`. 🔢🎨

Se quiser, o próximo passo lógico é:  
👉 `counter-increment` explicado separado  
👉 exemplos reais de uso  
👉 erro comum com `counter-reset`

É só falar 😄🚀