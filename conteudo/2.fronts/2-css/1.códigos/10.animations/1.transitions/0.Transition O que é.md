# Transition é o atributo que define transições suaves nos atributos dos elementos.

---

# 🌈 O que é **transition**?

Pense que um elemento na tela é como um brinquedo que pode **mudar de cor**, **mudar de tamanho**, **mudar de posição**…  
A propriedade **transition** é o _comando mágico_ que diz:

👉 “Quando alguma coisa mudar nesse elemento, **não mude de uma vez só**… **mude devagarinho**, de forma suave.”

É como quando você acende uma luz devagar, em vez de ligar de uma vez. 💡✨

---

# 🧩 O que o `transition` controla?

A propriedade **transition** é um “pacote” que junta quatro coisas:

1. **transition-property** → _o que vai mudar_ (cor? tamanho? borda?)
    
2. **transition-duration** → _quanto tempo leva para mudar_
    
3. **transition-timing-function** → _como essa mudança acontece_ (rápido? lento? suave?)
    
4. **transition-delay** → _espera um tempinho antes de começar?_
    

E a propriedade **transition:** sozinha permite colocar tudo isso em **uma única linha**.

---

# 🎁 Exemplo para você visualizar:

### **Sem transition:**

O botão muda de cor **instantaneamente**.

### **Com transition:**

O botão muda de cor **suavemente, como um fade**, usando o tempo que você escolheu.

```css
button {
  background: blue;
  transition: background 1s ease;
}

button:hover {
  background: red;
}
```

Quando você passa o mouse, ele troca de azul para vermelho **em 1 segundo**, suavemente.

---
 
