# Transition-property é o atributo que define qual atributo vai transacionar suavemente.

---

## 🧒 Explicação bem simples

Imagine que um elemento na tela é como um **bonequinho**.  
Quando você muda alguma coisa nele — como a cor, o tamanho ou a posição — essa mudança pode acontecer:

- **De uma vez só** (PÁ!), ou
    
- **De forma suave**, como se fosse uma animação.
    

A propriedade **transition-property** diz **QUAL parte do bonequinho deve mudar suavemente**.  
É como dizer:

> “Ei, só a cor vai mudar devagar! O resto muda rápido mesmo!”

---

## 🧪 Exemplo super simples

```css
div {
  transition-property: background-color;
  transition-duration: 1s;
}

div:hover {
  background-color: blue;
}
```

Aqui, **só a cor de fundo** vai mudar suavemente quando você passar o mouse.  
Se você mudasse a largura, ela mudaria na hora, sem animação, porque **não está listada** em `transition-property`.

---

## 🎨 Você pode colocar mais de uma propriedade

```css
transition-property: width, height, background-color;
```

Isso significa que **essas três coisas** vão mudar com animação.

---
