# **`@property`** é a **diretiva** que define **uma variável CSS customizada com suporte a transições e tipos de valor específicos**, permitindo criar animações e estilos dinâmicos.

























 # syntax é o atributo da diretiva @property que define o tipo da variavel.















 
# `inherits` é o atributo da diretiva @property que define se a variável **é herdavel ou não.
















# `initial-value` é o atributo da diretiva @property que define  o valor inicial da variável, antes de mudar.




















---

## 📌 O que é `@property` no CSS?

### 👉 Definição simples:

**`@property` é uma regra do CSS que serve para criar variáveis CSS “inteligentes”**, dizendo ao navegador **qual é o tipo do valor**, **se herda** e **qual é o valor inicial** 🎛️✨

> Pense assim:  
> **`@property` ensina o navegador como uma variável funciona** 🧠

---

## 🤔 Mas já não existe variável CSS?

Sim! Existe:

```css
:root {
  --cor: red;
}
```

👉 Isso funciona, **mas o navegador não sabe que tipo de valor é esse** (cor? número? ângulo?) 🤷‍♂️

E por causa disso:  
❌ não dá para animar direito  
❌ o navegador trata tudo como texto

---

## ✅ O que o `@property` resolve?

Com `@property`, você diz:

✔️ que tipo de valor é  
✔️ se herda dos pais  
✔️ qual o valor padrão

Assim o navegador entende **de verdade** a variável 💡

---

## 🧱 Estrutura do `@property`

```css
@property --nome-da-variavel {
  syntax: <tipo>;
  inherits: true | false;
  initial-value: valor;
}
```

---

## 🧩 Explicando cada parte (bem simples)

### 🔹 `--nome-da-variavel`

É o nome da variável (tem que começar com `--`) 🏷️

---

### 🔹 `syntax`

Diz **que tipo de valor** a variável aceita 👇

Exemplos comuns:

| Tipo           | Significado            |
| -------------- | ---------------------- |
| `<number>`     | números                |
| `<length>`     | tamanhos (`px`, `rem`) |
| `<percentage>` | porcentagem            |
| `<color>`      | cores                  |
| `<angle>`      | graus (`deg`)          |

---

### 🔹 `inherits`

Diz se a variável **passa do pai para o filho** 👨‍👦

- `true` → herda
    
- `false` → não herda
    

---

### 🔹 `initial-value`

Valor padrão da variável, caso ninguém defina 🚀

---

## 🎨 Exemplo simples (com cor)

```css
@property --minha-cor {
  syntax: <color>;
  inherits: true;
  initial-value: blue;
}
```

Uso:

```css
div {
  color: var(--minha-cor);
}
```

👉 Se ninguém mudar a variável, o texto será **azul** 🔵

---

## 🌀 Exemplo importante: animação (onde o `@property` brilha)

Sem `@property`, isso **não anima direito** 😢  
Com `@property`, funciona lindo ✨

```css
@property --rotacao {
  syntax: <angle>;
  inherits: false;
  initial-value: 0deg;
}

.caixa {
  width: 100px;
  height: 100px;
  background: red;
  transform: rotate(var(--rotacao));
  transition: --rotacao 1s;
}

.caixa:hover {
  --rotacao: 180deg;
}
```

🎉 Agora o navegador sabe que `--rotacao` é um **ângulo**, então ele anima certinho 🔄😍

---

## 🧠 Resumo bem curtinho

👉 **Definição técnica:**  
`@property` registra uma variável CSS tipada, permitindo animações e melhor interpretação pelo navegador 

👉 **Em palavras simples:**  
É como dizer ao navegador:

> “Ei! Essa variável é um número / cor / ângulo. Trate ela direito!” 😄🧠

---

Se quiser, posso te explicar:

- quando **não** usar `@property` 🚫
    
- lista completa de `syntax` 📋
    
- diferença entre `@property` e `:root` 🤯
    
- suporte nos navegadores 🌍
    

É só falar 😄👍