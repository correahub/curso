# JavaScript é uma linguagem de programação usada no navegador para tornar páginas HTML interativas e dinâmicas.





















### 📌 JavaScript

Ela roda no navegador (como o Google Chrome) e também no servidor com o Node.js.

---

### 🧠 Em termos simples:

Se o **HTML** cria a estrutura da página  
E o **CSS** cuida do visual  
👉 **JavaScript** controla o comportamento.

---

### 💻 Exemplo simples:

```html
<button onclick="mostrarMensagem()">Clique aqui</button>

<script>
  function mostrarMensagem() {
    alert("Olá, mundo!");
  }
</script>
```

Quando você clica no botão, o JavaScript executa a função e mostra uma mensagem.

---
